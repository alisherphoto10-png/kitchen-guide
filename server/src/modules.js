// Реестр модулей (платных функций) заведения. Новый модуль = новая запись
// здесь + requireModule('<key>') на сервере + useModule('<key>') во фронтенде.
// Базовые калькуляции (карточки ТТК, поиск, состав) модулями не являются
// и работают всегда.
const MODULES = {
  recalc: {
    title: 'Пересчёт',
    description: 'Калькулятор ТТК: множитель, на N порций, на нужный выход, от количества продукта. Выгрузка PDF/Excel с пересчётом.',
    defaultEnabled: false,
  },
};

// Единый текст для заблокированной функции — и в API, и в интерфейсе.
const LOCKED_MESSAGE = 'Доступно в расширенном тарифе — обратитесь к администратору';

module.exports = { MODULES, LOCKED_MESSAGE };
