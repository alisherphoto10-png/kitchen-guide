# Отчёт — Часть 10 реализована и задеплоена

Реализацию делала сессия с доступом к серверу (по формулировке задачи из
`PART-10-employee-schedule-visibility.md`). Ниже — что она сообщила по
факту, сверено с планом.

## Бэкенд (`/root/kitchendesk/backend`, задеплоено, `pm2 restart kitchendesk`)

- `schedule-import` теперь пишет время смены и роль **рядом** с
  `is_working` (аддитивно, как и требовалось — существующая логика не
  тронута). Роль резолвится через `iiko.getEmployeeRoles`, с fallback на
  пустой список, чтобы отсутствие ролей не ломало сам импорт.
- `GET /api/schedule/today` и `GET /api/schedule/colleagues?date=` — оба
  живьём проверены `curl`'ом с реальным JWT на demo-стенде
  (`restaurant_id=1`): пустое состояние без импорта, полное состояние
  после тестовой вставки. **Явно подтверждено — роль/грейд нигде не
  отдаётся** (ключевое требование по зарплатной чувствительности этой
  метки, см. Часть 10). Тестовые строки после проверки удалены из БД.

## Фронтенд (собран и задеплоен через `rsync --delete`, с сохранением
`oko-order`/логотипа — то есть без случайного затирания соседних частей
статики)

- `Dashboard.tsx` — карточка своей смены под бейджами в шапке
  приветствия, три состояния (заглушка / выходной / время), тап
  открывает шторку с неделей.
- `Profile.tsx` — блок «Коллеги по цеху сегодня» рядом с карточкой
  «Информация».

Совпадает с решениями из Части 10: разделение экранов, тап-раскрытие
недели, заглушка на время до готовности данных, скрытие роли/грейда.

## Отклонение от спецификации — опоздание и время открытия смены

**Не реализовано.** Причина подтверждена явно и конкретно (не
предположение) — реализующая сессия проверила `Checklist.tsx`/`shiftApi`
по инструкции «не изобретать на ходу» и нашла: там фиксируется **только
закрытие** смены (`POST /api/shift/finish`), никакого timestamp'а
**открытия** нигде не пишется — `shiftDone` живёт исключительно в
`localStorage` на клиенте, даже не долетает до сервера/БД. То есть дело
не в том, что не заметили или не успели — механизма зафиксировать момент
открытия смены **не существует вообще**, ни на клиенте (кроме локального
флага), ни тем более на сервере. Не подделали расчёт на пустом месте —
честно оставили нереализованным.

**Обновление (Часть 12, `PART-12-shift-open-lateness.md`)**: разведали
путь зафиксировать это через саму iiko («личная смена сотрудника») —
не подошёл, это plugin-API кассового терминала, не HTTP-эндпоинт, плюс
кассовый контур, который проект сознательно обходит с Части 8. Вернулись
к варианту фиксировать на стороне KitchenDesk (кнопка «Начать смену» /
автоматически по первому открытию вкладки) — но пользователь решил
**не делать вообще**: не все сотрудники открывают смену через приложение
одинаково, значит момент открытия — ненадёжный сигнал для «опоздания» в
принципе, а не то, что можно допилить точнее. **Опоздание и время
открытия смены исключены из планов окончательно**, не «отложено на
потом».

## Часть 11 — не начиналась

Общий недельный график для `chef`/`sushef`/`admin` (см. «Часть 11» в
`PART-10`) реализующая сессия **не трогала** — как и было явно оговорено
в формулировке задачи. Не самовольно расширили scope.

## Что дальше

- Проверить визуально, как это реально выглядит на экране (следующий шаг
  в той сессии — скриншот через `claude-in-chrome`).
- Часть 10 по своей исходной цели (время смены + коллеги по цеху,
  заглушка, тап-неделя) — **закрыта, реализовано и задеплоено**.
- Опоздание/время открытия — **закрыто как «не делаем»** (Часть 12), не
  зависает как недоделанный пункт и не всплывёт снова как забытая задача.
- Часть 11 (недельный график для администрации) — своим чередом, когда
  дойдёт очередь, не затронута этой реализацией.
