# Print Agent — оперативный контекст (читать новой сессии в первую очередь)

Этот файл — не техническая документация (она в других местах, ссылки в
конце), а введение в контекст: чтобы любая новая сессия Claude — что эта,
работающая в репозитории `kitchen-guide`, что "та, другая", с доступом к
серверу — за один файл поняла, что вообще происходит, и не заставляла
пользователя пересказывать всё заново.

## Что это вообще такое

В заведении есть настоящий термопринтер (Enter.CO SX-82V), подключённый по
локальной сети к моноблоку (Windows). Бэкенд KitchenDesk — в облаке и до
принтера в локальной сети заведения физически не достаёт. Решение —
локальный агент, `oko-shelf-life/agent/print-agent.js`: работает прямо на
моноблоке, постоянно в фоне (без окна на экране), раз в несколько секунд
спрашивает у облачного бэкенда "есть что печатать?" и сам шлёт байты
принтеру по сети (ESC/POS, порт 9100).

## Архитектурный принцип: "тупой агент, умный сервер"

Содержимое чеков (что и в каком порядке печатать) собирается ЦЕЛИКОМ на
сервере — агент просто берёт присланные строки (`job.printLines`) и
опциональный QR (`job.qrData`) и кодирует их в байты для конкретного
принтера. Агент отвечает только за физику печати (кодировка кириллицы,
отступы, форматирование, QR-команда) — не за то, ЧТО написано на чеке. Так
почти любая правка содержимого чеков — это правка сервера, без единого
прикосновения к моноблоку.

С 2026-09-20 то же самое верно и для КОДА самого агента: он умеет
самообновляться (см. ниже) — значит теперь и правки формата/логики агента
тоже не требуют физического похода к моноблоку, кроме самого первого раза.

## Реальные факты о деплое (легко перепутать, уже путались)

- Основной бэкенд KitchenDesk (план/чек-лист/смена) — на сервере это
  `/root/kitchendesk/backend`, единый процесс, pm2-имя `kitchendesk`, порт
  3004, домен `kitchendesk.chefplan.ru`.
- `kitchen2026.chefplan.ru` (порт 3001, процесс `oko-kitchen`) — это
  ОСТАНОВЛЕННЫЙ дубликат-легаси, НЕ второй сервер. Изначально фича печати
  чек-листа планировалась как межсерверный вызов backend → backend именно
  потому что предполагалось два разных сервера — по факту это оказался
  один процесс, см. `oko-checklist-print/README.md`.
- Очередь печати (`oko-shelf-life-print`, роуты `/api/oko-shelf-life-print/
  *`) живёт в ТОМ ЖЕ процессе `kitchendesk`. Значит вызовы "поставить
  задание на печать" из другой части главного бэкенда (например, при
  сохранении плана) — просто вызов функции внутри процесса, никакого HTTP
  между серверами не нужно.
- Реальный принтер: Enter.CO SX-82V, IP `192.168.0.122`, порт 9100,
  кодовая страница CP866 = номер 17 (подобрано эмпирически на реальном
  железе через `node print-agent.js --codepage-test`, для другого принтера
  может быть другой номер).

## Текущая версия агента

Смотреть `AGENT_VERSION` в начале `oko-shelf-life/agent/print-agent.js` —
на 2026-09-23 (последняя правка) это `"2026-09-23.1"`.

## Что уже сделано (коротко и по порядку; подробности — в README модулей)

1. Базовая печать этикеток разморозки/заморозки — справочник сроков
   хранения заготовок, `oko-shelf-life`.
2. Поддержка QR-кода в ESC/POS (`GS ( k`) — сделано заранее, впрок, для
   будущей фичи чек-листа (см. п.9).
3. Дизайн чека собирается целиком на сервере (`buildPrintLines` и т.п.) —
   агент просто печатает присланные строки, ничего не сочиняет сам.
4. Автозагрузка на Windows без видимого окна консоли (`.vbs`,
   `WshShell.Run(...,0,False)`) — решает "агент не запущен после
   перезагрузки, а окно консоли на экране никто не хочет видеть".
5. Чек-подтверждение при каждом старте агента — физическое доказательство
   "агент жив", раз окна на экране не видно и в логи не заглянешь.
6. Отступ перед обрезкой бумаги увеличен (было 3 пустые строки — резало
   слишком близко к тексту/QR, неудобно было взять чек в руки; стало 8).
7. Самообновление — агент сам, при каждом старте и раз в 6 часов, сверяет
   свою версию (`AGENT_VERSION`) с версией на сервере (роуты
   `/api/oko-shelf-life-print/agent-version` и `/agent-latest`, тот же
   `X-Agent-Token`, что и у остальных агентских роутов). Если на сервере
   новее — скачивает код, переписывает СЕБЯ (`fs.writeFileSync(__filename,
   ...)`), перезапускается. Местные настройки (`agent-config.json` —
   реальный токен, IP принтера, отступ перед обрезкой) — отдельный файл
   рядом, самообновление код его не трогает. Перед перезапуском печатает
   чек "Агент обновлён / Версия: ...".
8. Шапка "KitchenDesk" на любом чеке — жирным, по центру, крупнее
   (`ESC a 1` / `ESC E 1` / `GS ! 1`) — срабатывает автоматически, если
   первая строка `printLines` дословно `"KitchenDesk"`, остальной текст не
   затронут.
9. Фича "печать чек-листа смены при сохранении плана + завершение сканом
   QR" (`oko-checklist-print/README.md`) — использует ЭТОТ ЖЕ агент без
   единой правки: генерическая поддержка произвольных `printLines`/
   `qrData` в очереди печати была добавлена заранее именно ради этого.
10. Разведка второго принтера (Xprinter XP-235B, по USB, для этикеток со
    штрихкодом) — агент один раз выполняет `wmic printer get ...` и шлёт
    сырой результат на сервер (`POST /agent-report`), не заставляя
    пользователя работать с командной строкой самому. Ждём результата (см.
    "Открытые вопросы" ниже).
11. Печать тикета заказа (`oko-order-relay/README.md`) — при отправке
    заказа через форму (заведения "Облако"/"Мясо") печатается бумажный
    тикет, той же очередью/агентом, никакого дублирования. Требует
    проверки реального пути до `oko-shelf-life-store.js` при деплое (файл
    `oko-order-api.js` копируется в другое место бэкенда, не туда же, где
    чек-лист).
12. Страница настроек чекового принтера (`http://localhost:3500`, только
    на самом моноблоке) — агент сам поднимает обычную веб-страницу
    (встроенный `http`, без фреймворков): IP/порт принтера, кодовая
    страница, отступ перед обрезкой, оформление шапки "KitchenDesk" (три
    независимых флага, раньше были зашиты намертво). Сохранение применяется
    сразу, без перезапуска. Плюс кнопки "Тестовая печать"/"Тест кодовых
    страниц" — сразу видно результат. Первый шаг к "полноценному
    приложению", о котором просил пользователь — начали с чекового
    принтера, т.к. по нему уже всё известно; для этикеточного такая же
    страница имеет смысл после разведки (см. п.10) и выбора протокола.
13. Многозаведенческая печать — очередь и агентский токен теперь привязаны
    к заведению (не один общий секрет на всех), у каждого заведения свой
    список принтеров (несколько станций на кухне поддерживаются —
    `printerTarget` на задании, сервер сам решает, куда слать). Управляется
    веб-панелью `/print-admin/` (`oko-shelf-life/frontend/
    restaurants-admin/index.html`) — заведения добавляются, токены
    выпускаются и принтеры настраиваются оттуда, без правки сервера или
    переноса файлов на конкретный моноблок. Легаси-заведение (ОКО) не
    трогали — старый `OKO_SHELF_LIFE_AGENT_TOKEN` продолжает работать как
    раньше. Агент печатает туда, куда скажет сервер в задании
    (`job.printerIp`/`printerPort`); свой локальный `PRINTER_IP` остаётся
    только как "тестовый принтер" для кнопок настройки и запасной вариант
    для легаси-заведения. `oko-order-relay` подключён (`restaurantId:
    order.venue`); чек-листу (`oko-checklist-print`) это ещё нужно
    прокинуть отдельно — см. "Открытые вопросы" в его README.
    **Найден и исправлен реальный баг вскоре после первого деплоя:**
    задание для ещё не заведённого в панели `restaurantId` становилось
    невидимым ни для одного токена (не "падало в легаси", как ошибочно
    предполагал первый деплой) — несколько минут реальные тикеты заказов
    терялись, поймали на живом сервере и откатили. Исправлено в сторе
    (`resolveRestaurantId()` перед сохранением любого задания — см.
    `oko-shelf-life/README.md`), не откатом строки в `oko-order-relay`.
14. Синхронизация списка заведений с настоящим реестром KitchenDesk
    (2026-09-20) — панель `/print-admin/` до этого сама была источником
    истины (заведение заводилось вводом названия в форме), что могло
    разойтись с реальными клиентами KitchenDesk. Пользователь явно
    потребовал синхронности: "В print агенте не должно быть других
    заведений, которые нету, которые не подключены к kitchen desk." Теперь
    список заведений в панели **читается напрямую из настоящей таблицы
    `restaurants`** (тот же процесс, `store.listKitchenDeskTenants()`,
    только активные — `deleted_at IS NULL`), панель только "включает"
    печать для существующего заведения (создаёт токен + список принтеров),
    завести несуществующее заведение больше нельзя. Print-agent остался
    отдельным модулем — таблицу заведений только читает, не пишет в неё и
    не владеет ей ("ничего внедрять не будем, это как новый модуль будет
    отдельный" — пользователь). Заодно легаси-заведение ОКО (раньше жило
    отдельной веткой по совпадению токена с переменной окружения, без
    записи в списке заведений вообще) переведено на эту же схему — стало
    обычной записью с реальным id (`1`, как в таблице `restaurants`),
    токен при миграции явно задан тем же значением, что уже в
    `OKO_SHELF_LIFE_AGENT_TOKEN`, чтобы уже работающий на моноблоке агент
    не пришлось переконфигурировать. Подробности и точная команда миграции
    — `oko-shelf-life/README.md`, раздел "Почему список заведений не
    вводится руками". **Задеплоено и подтверждено (2026-09-20):** путь до
    db-модуля основного бэкенда оказался не `../db/postgres`, а
    `./db/postgres` (`oko-shelf-life-store.js` лежит в `backend/src/`,
    рядом с `db/`, а не на уровень выше) — поправлено при деплое; миграция
    ОКО (`POST /restaurants/1/enable`) выполнена, `enabled: true`, легаси-
    токен продолжает работать без изменений на моноблоке, IP/порт принтера
    теперь приходят в задании от сервера. Демо-заведение (id 6) оставлено
    выключенным.
15. Скачивание готового `agent-config.json` из панели (2026-09-20) — до
    этого токен для нового заведения приходилось передавать текстом и
    вписывать в файл на моноблоке руками. Кнопка "⭳ Скачать файл настроек
    агента" на карточке включённого заведения отдаёт файл с уже
    подставленными `BACKEND_URL`/`AGENT_TOKEN` (и IP/портом первого
    принтера, если он уже добавлен в панели) — просто положить рядом с
    `print-agent.js` на моноблоке, агент подхватит сам при первом запуске.
    См. `oko-shelf-life/README.md`, п.4 в разделе "Заведения и принтеры".
16. Однокликовый запуск + PDF-инструкция для установщика на месте
    (2026-09-21) — выяснилось в разговоре с пользователем, что "три файла
    в папку, дальше руками команды в cmd" — не сценарий для человека без
    технических навыков, который физически подключает принтер в
    заведении. Добавлен `agent/start-agent.bat` — общий для всех
    заведений файл-лаунчер (не привязан к пути установки, `cd /d
    "%~dp0"`), двойной клик по которому одним действием прогоняет
    `node print-agent.js --install-autostart` и сразу запускает агента
    без окна. Итоговый комплект для установки — **три файла**:
    `print-agent.js` + `start-agent.bat` (оба общие, не меняются от
    заведения к заведению) + персональный `agent-config.json` (см. п.15).
    Плюс отдельный PDF `print-agent/docs/kitchendesk-chekovyi-printer-
    instrukciya.pdf` — пошаговая инструкция для не-технического человека
    на месте, со словарём терминов и реальными скриншотами панели и
    страницы настроек агента (не макеты). Только про чековый принтер.
17. Разведка подтверждена на реальном железе + диагностика этикеточного
    принтера (2026-09-22). Отчёт с моноблока ОКО пришёл: Xprinter XP-235B
    установлен в Windows как обычный принтер, порт `USB001`, драйвер
    определился штатно — но само ИМЯ принтера в отчёте вышло
    кракозябрами (`wmic ... /format:csv` криво отдаёт кириллицу через
    `exec()` — консольная кодовая страница Windows, а не UTF-8, тот же
    класс проблемы, что был с кодовой страницей чека). Заодно выяснилось
    важное: чековый Enter.CO в списке принтеров Windows вообще НЕ
    появился — потому что агент печатает на него напрямую по IP-сокету,
    минуя Windows целиком, а Xprinter, раз он зарегистрирован как
    Windows-принтер, требует другого способа отправки байтов — через
    очередь печати Windows с типом задания RAW, не просто TCP-сокет.
    Сделано:
    - `listWindowsPrinters()` — разведка переведена с `wmic` на
      `Get-CimInstance Win32_Printer` через PowerShell; результат
      PowerShell сам пишет в UTF-8-файл
      (`[System.IO.File]::WriteAllText(..., [System.Text.Encoding]::UTF8)`),
      Node читает готовый JSON — кодовая страница консоли Windows теперь
      вообще не участвует, кракозябров больше не будет.
    - `runUsbLabelTest()` — диагностика (не полноценная печать этикетки,
      калибровка TSPL ещё впереди): ищет Xprinter по подстроке в
      `DriverName`, шлёт минимальный TSPL-тест (40×30мм) через
      Windows-спулер по стандартной схеме `OpenPrinter`/
      `StartDocPrinter`/`WritePrinter` (winspool.drv, P/Invoke из
      PowerShell, тип задания `RAW` — байты долетают как есть, без
      обработки драйвером). Кнопка **"Тест этикеточного принтера"** на
      `localhost:3500` (плюс `node print-agent.js --usb-label-test` из
      командной строки) — результат виден и на экране моноблока, и
      уходит на сервер тем же каналом, что и разведка (`agent-report`,
      `type: "usb-label-test"`), чтобы можно было посмотреть удалённо.
    - **Не проверено на реальном железе** — весь Win32-код написан по
      стандартной, документированной схеме (классический пример
      Microsoft "raw printing via P/Invoke"), но не через реальный
      Windows/принтер, только синтаксически и на Linux-заглушке
      (PowerShell отсутствует — код корректно проваливается с понятной
      ошибкой, не падает). Первый реальный прогон на моноблоке ОКО может
      вскрыть нюансы (например, что кнопка не находит принтер, или
      `StartDocPrinter` падает с конкретным кодом ошибки) — для того и
      сделана диагностика, а не сразу "боевая" печать.
18. Найден и исправлен реальный баг диагностики (2026-09-22, в тот же
    день) — первый прогон на моноблоке ОКО после п.17 писал "принтер не
    найден", хотя PowerShell реально отдавал список принтеров без ошибок.
    Причина — `[System.Text.Encoding]::UTF8` в PowerShell всегда
    добавляет BOM при записи файла, Node это не срезал при чтении,
    `JSON.parse` падал на `﻿` в начале. Исправлено с двух сторон
    (PowerShell пишет без BOM + Node на всякий случай срезает BOM сам,
    см. `readJsonFileNoBom` в `print-agent.js`). Версия агента после
    фикса — `2026-09-22.3`. **Реальный результат теста на живом Xprinter
    после этого фикса в разговоре подтверждён не был** — это всё ещё
    открытый вопрос, см. ниже.
19. Мобильное приложение — решения приняты, реализация ещё НЕ начата
    (2026-09-22/23). Договорено с пользователем:
    - Обёртка вокруг существующего сайта KitchenDesk через **Capacitor**
      (не голый WebView вручную) — один проект под Android и, позже, iOS.
    - Собирать через **GitHub Actions** (у CI есть доступ к серверам
      Google/Android SDK, у этой песочницы — нет, прямая сборка здесь
      невозможна).
    - Защита с самого начала: ни одного секрета/токена внутри файла
      приложения, только HTTPS, WebView разрешено открывать только домен
      KitchenDesk, USB-отладка WebView выключена в релизной сборке,
      минимум разрешений (только интернет).
    - Пользователь сознательно отказался от единого `.exe`-инсталлятора
      для десктоп-агента по той же логике (риск экрана Windows
      SmartScreen на неподписанный файл хуже, чем нынешние три файла +
      один клик) — см. `oko-shelf-life/README.md` про `start-agent.bat`,
      прямого отношения к мобильному приложению это решение не имеет, но
      логика та же (см. п.16 там).
    - **Вход в приложении** — НЕ через Telegram (пользователь явно
      попросил минимизировать Telegram именно для входа, но НЕ для
      уведомлений/групп — их не трогаем, см. п.20). Придумана схема:
      разовый токен только для ПЕРВОЙ привязки устройства (сотрудник
      вводит его один раз при установке), дальше приложение держит
      обычную долгую сессию (тот же механизм, что уже есть для "Вход с
      ПК", см. п.20) — токен НЕ нужно вводить каждый раз. Поверх —
      PIN-код для быстрого повторного входа, переиспользуя уже
      существующий PIN-эндпоинт (не новая система).
    - Пользователь решил делать в первую очередь **доработку OKO-модулей
      под несколько заведений** (п.21) и только потом браться за
      приложение — порядка "недели-две" на модули, затем приложение.
20. **Security-аудит входа + фикс (2026-09-22/23, важное).** Пользователь
    боялся, что "ссылку в Telegram можно подставить и войти" — попросил
    проверить перед тем, как убирать/менять вход для приложения. Аудит
    (сделан той сессией, только чтение кода, `/root/kitchendesk/backend`)
    показал: сам вход через Telegram Mini App (подпись `initData` по
    HMAC-SHA256 ключом бота) подделать нельзя, страх в этой части не
    подтвердился. Но нашлась **настоящая дыра в другом месте**: код
    приглашения нового сотрудника был равен id заведения (1, 6 и т.д.) —
    угадывается, и вдобавок показывал список ВСЕХ ещё не привязанных
    профилей заведения на выбор. Кто угодно в Telegram мог занять чужой
    профиль повара (в ОКО было 2 таких свободных профиля).
    Исправлено и задеплоено (прод, 2026-09-23), **действующих сотрудников
    не задело** (сессии/пароли/PIN не трогали):
    - Код приглашения теперь **одноразовый, привязан к конкретному уже
      созданному профилю сотрудника** (кнопка "Получить код приглашения"
      в карточке сотрудника в "Управление → Сотрудники"), живёт 48 часов,
      список свободных профилей на выбор убран полностью.
    - Debug-логи с личными данными пользователей (`[AUTH DEBUG]` в PM2
      логах) выключены.
    - Лимит попыток входа (`/api/auth/login`, `/api/auth/pin`) был ОБЩИЙ
      на всех клиентов KitchenDesk разом (Express не различал IP за
      nginx) — теперь считается по IP каждого клиента отдельно. Заодно
      нашли и закрыли способ обойти это через подделку заголовка
      `X-Forwarded-For` — сервер теперь доверяет только `X-Real-IP` от
      nginx.
    - Заодно (не срочное, тоже сделано): лимит на вход по паролю в боте +
      уведомление прежнему владельцу при перепривязке аккаунта; PIN/пароли
      теперь генерируются через `crypto`, не `Math.random`.
    - Побочная находка: `deploy.sh` был бы `rsync --delete` без исключений
      для `shelf-life/`/`print-admin/` — при следующем деплое этим
      скриптом это стёрло бы обе фичи с прода. Исключения добавлены,
      реального удаления не произошло.
    - **Не решено:** ~34 тысячи старых строк с личными данными в
      `~/.pm2/logs/kitchendesk-out.log` — пользователь ещё не сказал,
      чистить их или оставить.
    - **Нужно сделать пользователю (операционное, не код):** выдать новые
      коды приглашения двум сейчас свободным профилям поваров в ОКО через
      новую кнопку.
21. Тарифы — начато, не завершено. Полная инвентаризация функционала
    KitchenDesk (63 позиции, все модули) сделана той сессией по реальному
    коду, лежит в `print-agent/docs/kitchendesk-funkcii-dlya-tarifov.md`.
    Из неё собран Excel `print-agent/docs/kitchendesk-funkcii-tarify.xlsx`
    (лист "Функции" с выпадающим списком "Тариф" на каждую строку, лист
    "Тарифы" редактируемый, лист "Не входит" справочный) — пользователь
    ещё не заполнил распределение по тарифам и не назначил цены. Важное
    из инвентаризации: модули "Пособие официанта", "Инвентаризация
    утвари", "Заказы Облако/Мясо" помечены `[OKO-модуль]` — работают
    только для одного заведения (ОКО), продавать другим клиентам нельзя
    без доработки под несколько заведений — это и есть п.19 выше
    ("доработка модулей", текущий приоритет пользователя).
22. Контент-план для соцсетей — начат заново "с нуля" (2026-09-22).
    Старый план ("Публикации ОКО", 13 постов, артефакт
    `bd318e99-2a3f-46cf-97cc-4bfd8baa2185`) свёрнут, напоминание-триггер
    удалён. Новая концепция согласована (не реализована постами): упор на
    алгоритм Instagram без Reels (пока пользователь не умеет снимать
    видео) — карусели как главный формат, акцент на сохранения/репосты,
    недельный ритм рубрик (боль/факт/до-после/цифры/кейс/закулисье/
    вовлечение), частота — вплоть до ежедневной. Конкретные тексты постов
    ещё не написаны — это следующий шаг, когда пользователь скажет
    продолжать.
23. Печатные версии плана работ (2026-09-22/23) — по просьбе пользователя
    ("чтобы не забыл, не хочу много писать сам") сделаны файлы для печати
    на домашнем/мини-принтере, все в `print-agent/docs/`:
    - `kitchendesk-plan-checklist.pdf` — A4, чек-лист всего плана целиком.
    - `plan-cards/kitchendesk-card-1..9.png` — по 1-3 связанных пункта на
      карточку, 160×55мм (горизонтальная ориентация, PNG — у пользователя
      Bluetooth-мини-принтер печатает только картинки, не PDF).
    - `kitchendesk-plan-strip-full.png` — весь план одной длинной
      вертикальной картинкой, 55×282мм.
    Эти файлы — снимок состояния на момент создания, НЕ обновляются
    автоматически при изменении плана — если план сильно уйдёт вперёд,
    нужно будет пересобрать заново (HTML-исходники в scratchpad этой
    сессии, не в репозитории — если понадобится пересобрать, проще
    сгенерировать заново по этому README, чем искать старые файлы).
24. Найден и исправлен второй BOM-баг (2026-09-23) — первый реальный
    прогон диагностики на `2026-09-22.3` (после фикса бага #1) продвинулся
    дальше: принтер теперь находился, но `OpenPrinter failed, error 1801`
    (`ERROR_INVALID_PRINTER_NAME`, подтверждено через WebSearch, не
    угадано). Причина — тот же класс бага, что и вчера, только на
    ЗАПИСИ: `runUsbLabelTest()` подставляет живое имя принтера в
    сгенерированный `.ps1`, PowerShell 5.1 без BOM в файле читает его в
    системной кодовой странице, а не UTF-8 — имя портилось ещё до вызова
    `OpenPrinter`. `listWindowsPrinters()` этот баг не задевал (там текст
    скрипта фиксированный ASCII), только `runUsbLabelTest()`, где
    подставляются реальные данные. Фикс — `runPowerShellScript()` теперь
    ВСЕГДА пишет `.ps1` с BOM в начале (проверено локально побайтово:
    `EF BB BF` на месте). Версия — `2026-09-23.1`.
25. **Подтверждено на реальном железе (2026-09-23): этикетка физически
    напечаталась.** Пользователь нажал "Тест" три раза на `2026-09-23.1` и
    прислал фото трёх напечатанных этикеток — оба BOM-бага закрыты, канал
    агент → Windows-спулер → USB-принтер работает end-to-end. Осталось
    не "чинить", а калибровать: на фото текст мелкий, прижат к нижнему
    краю и выглядит перевёрнутым — ожидаемо, т.к. в тестовом payload
    (`buildUsbLabelTestPayload()`) размер `40mm x 30mm` и позиция `TEXT
    20,20` подобраны наугад, реальные размеры этикетки не были известны.
    Пользователю заданы 2 вопроса: реальный размер этикетки (Ш×В в мм) и
    зазор между этикетками (мм) — ответ ещё не получен в разговоре.
    Как только придут — поправить `SIZE`/`GAP`/`TEXT` в
    `buildUsbLabelTestPayload()` (или сделать отдельную функцию для
    настоящего макета с названием/датой, а не тестовым текстом) и
    перепроверить ориентацию (параметр rotation в `TEXT` сейчас `0`).
26. Получены реальные размеры этикетки от пользователя (2026-09-23):
    58мм (ширина, альбомная ориентация) × 30мм (высота), зазор 3мм.
    `buildUsbLabelTestPayload()` обновлён: `SIZE 58 mm,30 mm`,
    `GAP 3 mm,0 mm`, текст поднят и укрупнён (x2,y2 для заголовка) —
    раньше был угадан размер 40×30мм с текстом у самого верха координат
    (`TEXT 20,20`), из-за чего на фото текст выходил мелким и у края.
    `AGENT_VERSION` → `2026-09-23.2`. Ориентацию (текст выглядел
    перевёрнутым на предыдущем фото) намеренно НЕ трогали в этом фиксе —
    сначала нужно посмотреть, не решится ли она сама с правильным
    размером/GAP (частая причина смещения — неверный SIZE путает датчик
    зазора и печать съезжает). Если на новом фото снова перевёрнуто —
    следующий шаг: rotation `180` вместо `0` в обеих командах `TEXT` (это
    четвёртый параметр, сейчас `0`), НЕ трогая остальные координаты.
    **Ещё не задеплоено на сервер и не проверено на живом принтере** —
    ждём деплоя (другая сессия) и нового теста.
27. Пользователь сделал калибровку принтера вручную (кнопка FEED при
    включении) и прогнал новый тест на `2026-09-23.2` — прислал фото.
    Хорошая новость: текст ПОЛНОСТЬЮ помещается на этикетке (калибровка
    помогла) и ориентация оказалась верной (лицом вперёд, не перевёрнуто
    — моя гипотеза про переворот была ОШИБОЧНОЙ, пользователь поправил).
    Проблема другая: строка "TEST 58x30mm" (шрифт "2", без множителя)
    пропечаталась чётко, а строка "KitchenDesk" (шрифт "3" с множителем
    `x2,y2`) вышла блёклыми точками, почти нечитаемо — похоже, этот
    дешёвый TSPL-клон плохо поддерживает масштабирование встроенного
    шрифта через x-mult/y-mult. Фикс — убрал множитель, вместо
    увеличения через множитель взял готовый крупный встроенный шрифт "4"
    (`TEXT 30,50,"4",0,1,1,"KitchenDesk"`) без масштабирования.
    `AGENT_VERSION` → `2026-09-23.3`. **Ещё не задеплоено и не проверено
    на живом принтере.**
28. Пользователь обновил агент до `2026-09-23.3` и прислал новое фото.
    Смена шрифта (п.27) НЕ помогла — строка "KitchenDesk" вышла ровно
    так же блёкло, шрифтом "4" вместо "3", в том же месте (проверено
    побайтово: новое фото и старое — РАЗНЫЕ файлы, то есть это
    действительно новый тест, не повторно отправленное старое фото). Раз
    смена шрифта ничего не изменила, а вторая строка ("TEST 58x30mm",
    код которой не менялся между версиями) стабильно печатается чётко —
    дело не в шрифте, а, видимо, в прожиге/плотности. Через WebSearch
    подтверждён TSPL `DENSITY n` (0-15, по умолчанию 8, чем больше — тем
    темнее печать). Добавлена явная `DENSITY 15` в
    `buildUsbLabelTestPayload()`. `AGENT_VERSION` → `2026-09-23.4`.
    **Ещё не задеплоено и не проверено на живом принтере.**
29. Пользователь спросил, можно ли сделать так, чтобы принтер сам
    "посмотрел на себя" и сказал, что с ним не так (в частности, не
    повреждена ли печатающая головка физически) — отдельная задача от
    калибровки тестовой этикетки. Через WebSearch нашёл, что в TSPL есть
    штатная команда `SELFTEST` с параметрами: без параметра — этикетка с
    полной информацией о принтере (модель, DPI, текущие настройки и
    т.п.); `SELFTEST PATTERN` — тестовый узор специально для проверки
    нагревательной линии головки (не хватает ли отдельных элементов —
    видно по постоянным белым пропускам на одном и том же месте на
    каждой этикетке). Это настоящая печатная самодиагностика из
    прошивки принтера, без необходимости зажимать кнопку FEED при
    включении. Реализовано в `2026-09-23.4`:
    - Вынес общую логику отправки сырых байт на USB-принтер
      (P/Invoke на `winspool.drv` через generated `.ps1`) из
      `runUsbLabelTest()` в переиспользуемую `sendRawBytesToXprinter()`
      — чтобы не дублировать этот довольно большой кусок кода для
      второго вида задания.
    - Новая `buildPrinterSelfTestPayload()` — `SELFTEST\r\nSELFTEST PATTERN\r\n`.
    - Новая `runPrinterSelfTest()` — использует общую функцию, шлёт
      отчёт на сервер с `type: "usb-printer-selftest"` (тот же
      `agent-report` эндпоинт, тип отличает эту диагностику от обычного
      теста этикетки).
    - Новая кнопка "Самотест принтера (проверка головки)" на странице
      настроек (`localhost:3500`), рядом с существующей кнопкой теста
      этикетки. Плюс CLI-флаг `--usb-printer-selftest`.
    - Важная оговорка (сказана и пользователю): это НЕ гарантированно
      сработает — дешёвые клоны TSPL реализуют не все команды из
      спецификации. Сам факт того, сработает команда или нет, — уже
      диагностическая информация.
30. Пользователь прислал фото по `2026-09-23.4`: `SELFTEST`/`SELFTEST
    PATTERN` дали ДВЕ ПУСТЫЕ этикетки (клон их не поддерживает, как и
    предполагалось в п.29) — неинформативно. И `DENSITY 15` (п.28) тоже
    НЕ помогла — строка "KitchenDesk" всё так же блёклая, в том же месте,
    несмотря на максимальную плотность. Раз ни смена шрифта, ни
    максимальная плотность не изменили результат — пора проверять
    однозначно, дело в физическом дефекте головки или нет.
    `buildPrinterSelfTestPayload()` дополнен: после (неудачных) SELFTEST
    команд добавлен НАШ СОБСТВЕННЫЙ, гарантированно интерпретируемый
    тест — команда `BAR 0,0,464,240` заливает всю этикетку сплошным
    чёрным прямоугольником. Это самый прямой способ отличить "дело в
    контенте/командах" от "дело в самой головке": сплошная точная
    заливка без пропусков = голова исправна; белая вертикальная полоса
    на одном и том же месте на каждой копии = физически повреждённый
    нагревательный элемент. `AGENT_VERSION` → `2026-09-23.5`. **Ещё не
    задеплоено и не проверено на живом принтере.**
31. Пользователь прислал результат по `2026-09-23.5`: "Белый лист" —
    ПУСТО, даже гарантированный `BAR` не пропечатался. Вывод: дело не в
    самом `BAR`, а в том, что `SELFTEST`/`SELFTEST PATTERN` (которые
    этот клон не понимает) шли В ОДНОМ задании перед ним и, похоже,
    сбивают разбор остатка задания — всё, что идёт после непонятных
    команд, теряется целиком. Убрал `SELFTEST`/`SELFTEST PATTERN` из
    `buildPrinterSelfTestPayload()` полностью — теперь это задание шлёт
    ТОЛЬКО чистый, изолированный `BAR`, без риска побочных эффектов от
    неподдерживаемых команд. `AGENT_VERSION` → `2026-09-23.6`. **Ещё не
    задеплоено и не проверено на живом принтере.**
32. Пользователь прислал результат по `2026-09-23.6`: СНОВА пусто — уже
    изолированный (без SELFTEST) `BAR` тоже ничего не напечатал.
    Гипотеза "SELFTEST ломает разбор" не подтвердилась. Но ключевой
    факт: строка "TEST 58x30mm" командой `TEXT` печаталась ЧЁТКО и
    стабильно много раз подряд ДО этого — если бы голова была физически
    повреждена, `TEXT` тоже не печатал бы. Значит дело не в голове, а
    именно в команде `BAR` — либо этот клон её не поддерживает вообще,
    либо синтаксис `BAR x,y,width,height` для него не тот (документация
    была не полностью подтверждена — только общие фрагменты через
    WebSearch, PDF-мануалы недоступны из-за блокировки egress-прокси на
    эти домены). Вместо непроверенной команды — проверяем голову ТЕМИ ЖЕ
    средствами, что уже подтверждённо работают: `buildPrinterSelfTestPayload()`
    переписан на несколько строк `TEXT` тем же шрифтом "2", что дал
    чистый "TEST 58x30mm", плотно заполненных повторяющимся "X" на всю
    высоту этикетки (8 строк по 35 символов). Не идеальная сплошная
    заливка, но плотная и построена ТОЛЬКО из уже проверенных
    строительных блоков (TEXT/SIZE/GAP/DENSITY/CLS/PRINT), без риска
    зависимости от неподтверждённой команды. `AGENT_VERSION` →
    `2026-09-23.7`. **Ещё не задеплоено и не проверено на живом принтере.**
33. Пользователь протестировал `2026-09-23.7` и словами описал результат:
    "не полностью Х а на половину буквы и так вся строка" — то есть у
    каждой буквы "X" пропечатана только половина, и так по всей строке.
    Фото не присылал, я попросил прислать для точного разбора (важно
    различить "обрезана верхняя/нижняя половина у КАЖДОЙ буквы одинаково
    на ВСЕХ строках" — похоже на неверную настройку высоты, а не на
    поломку — от "пропуск на одном и том же месте по ширине на каждой
    строке" — это уже физический дефект головки). Пользователь фото не
    прислал и переключился на другую задачу (печать ТТК, см. ниже) — тема
    ремонта принтера открыта и НЕ доведена до вывода. Дальше по этой теме
    в разговоре не возвращались.
34. Параллельно пользователь запустил отдельную задачу — печать ТТК
    (техно-технологической карты) для поваров на ЧЕКОВОМ принтере (не
    этикеточном), чтобы не лезть в телефон постоянно у плиты. Разведку
    реального устройства ТТК в БД сделала другая сессия (файл
    `print-agent/docs/ttk-ustroistvo-dlya-pechati.md`, коммит `2e9ccb0`,
    подробно: какие поля заполнены реально в ОКО, что видит повар на
    экране, откуда брать цех/повара для печати). На основе разведки
    согласован и реализован (другой сессией, бэкенд/фронтенд не в этом
    репозитории и не в git) макет печати ТТК:
    - `POST /api/ttk/:id/print`, вёрстка в `src/utils/ttkPrint.js`,
      печать идёт через ту же очередь, что тикеты заказов и чек-лист
      смены.
    - Граммовка — брутто (как видит повар на экране). Технология
      (`cooking`) НЕ печатается в v1 (задел — параметр `includeCooking`,
      сейчас всегда `false`). Цех/повар — от того, кто реально нажал
      кнопку (его профиль), не из карты (там пусто у 92% карт).
    - Кнопка видна поварам/сушефам/шефам/админам (НЕ суперадмину). Если
      повар занят — коллега с доступом просто нажимает сам, никакой
      отдельной логики "запроса печати" нет.
    - Печать учитывает пересчёт из калькулятора порций: если пересчёт
      был сделан перед печатью — печатаются пересчитанные цифры (плюс
      отдельная строка "Пересчёт: xN", чтобы не путать с базовой
      порцией), не пересчитывали — печатается база.
    - Печать разрешена только заведениям, подключённым в `/print-admin/`
      (сейчас только ОКО) — иначе чужой рецепт ушёл бы на принтер ОКО.
    - Отклонения от макета: `≈` печатается как `~` (принтер знает только
      кириллицу/латиницу), кавычки-ёлочки и тире — на обычные, граммовка
      в тех единицах, что на экране (не всегда граммы).
    - Проверено: вёрстка на реальных карточках из базы (ни одна строка
      не длиннее 32 символов на 58мм-ленте), отказы эндпоинта (409
      демо-заведение без печати, 403 чужая ТТК, 404 несуществующая, 400
      мусор во входных данных, 401 без входа).
    - **Реальная физическая печать ЕЩЁ НЕ проверена** — специально не
      запускали тест, чтобы не вывести тестовый чек на раздачу ОКО.
      Первое нажатие кнопки "Печать" в реальной работе и будет первым
      физическим тестом.
    - Попутно найдены (не в рамках этой задачи, не трогали пока) два
      независимых бага печати: (а) чек-лист смены, вероятно, вообще не
      печатается — `plan.js` ставит задание без `restaurantId`, попадает
      в старую общую очередь, которую агент, похоже, больше не читает
      после перевода ОКО на id `"1"` (не подтверждено вживую — чек-листов
      после 20.09 не было); (б) тире "—" в тикетах заказов печатается
      как "?" (нет в таблице символов агента). Пользователь попросил
      исправить оба сейчас же — задача поставлена другой сессии, ответ
      ещё не получен в разговоре.
35. Пользователь поправил: чековая лента у SX-82V не 58мм, а **80мм** —
    значит вся оценка ширины строки в п.34 (32 символа) была неверной с
    самого начала (моя ошибка при постановке задачи, не проверил
    физический размер ленты, взял на веру). Передал исправление другой
    сессии с явной просьбой НЕ гадать число символов, а проверить
    по факту/сверить с уже работающими шаблонами.
36. Другая сессия закрыла все три правки разом:
    - **Чек-лист смены** — баг подтверждён и исправлен: `plan.js` теперь
      кладёт задание в очередь заведения, которому принадлежит план (для
      ОКО — id `"1"`), а не в старую безымянную очередь, которую агент
      больше не читает. Если у заведения печать не подключена — задание
      и QR-токен теперь вообще не создаются (раньше ушли бы в никуда).
      Проверено на подменённой БД/очереди/боте (без затрагивания живых
      данных): для ОКО задание ушло в очередь "1", для демо-заведения
      без печати — не создалось ничего.
    - **Тире в тикетах** — исправлено в агенте (`AGENT_VERSION` →
      `2026-09-23.8`, коммит `b222119`): "—"/"–" → "-", заодно тот же
      список замен, что уже был у ТТК-печати (ёлочки → `"`, `≈` → `~`,
      `№` → `N`, `×` → `x`). Длинного тире и `≈`/`№` в CP866 либо нет
      совсем, либо есть, но непроверено на этом принтере — обычная
      латиница гарантированно работает, взяли её. Проверено прогоном
      функции перевода текста в байты отдельно от агента.
    - **Ширина ленты** — вместо угаданных 48 символов (576 точек
      головки при 203dpi на 80мм) поставили **42** — осторожно, потому
      что паспорт принтера подтверждает только "80мм, 203dpi", а точное
      число точек головки (576 или 512) без замера неизвестно. При 42
      переломанная строка возможна только если ошиблись В МЕНЬШУЮ
      сторону, что безопаснее полей. Способ проверить на 48 позже:
      посмотреть на разделитель `=====` на следующем реальном чеке —
      если не доходит до правого края примерно на 1см, значит реально
      48, можно поднять `WIDTH` в `src/utils/ttkPrint.js`. Вёрстка
      перепроверена по всем 351 активной ТТК ОКО при ширине 42 — ни
      одна строка не длиннее 42 символов, обычная длина чека 16 строк,
      максимум 28.
    - **Попутно подтверждено: печать ТТК уже реально работает в бою** —
      повар Камиль дважды напечатал "Баклажан с Тахини и помидорами"
      (15:08 и 15:09), агент оба раза отметил задание выполненным. Те
      два чека ушли ещё со старой шириной 32 (до фикса), новые пойдут
      с 42. Специального "первого теста" решили не делать (чтобы не
      выводить лишний чек на раздачу) — но по факту он уже случился
      сам, в реальной работе.
    - Обновил (эта сессия) устаревшее "58мм" в
      `print-agent/docs/ttk-ustroistvo-dlya-pechati.md` — добавлена
      пометка-исправление сверху файла, сам текст разведки (поля в БД,
      что видит повар) не трогал, он остаётся верным.
37. Отдельная от печати задача, но тот же день: сломалась рассылка на
    панели суперадмина (составил текст+фото, нажал "отправить всем",
    вылезла ошибка, плашка исчезла раньше, чем успели посмотреть текст).
    Разобрала другая сессия: НЕ баг в смысле сломанного кода — текст был
    на 2 символа длиннее лимита Telegram для подписи к фото (1024
    символа), все 18 адресатов получили отказ, а панель показала
    "Отправлено: 0, ошибок: 18" зелёной плашкой, которая быстро исчезла
    — это и приняли за необъяснённую ошибку. Найдено 5 реальных проблем
    (не только у суперадмина, та же дыра у обычного админа заведения):
    длинный текст с фото падает вместо разделения на два сообщения; нет
    счётчика символов; сервер не возвращает причину ошибки; ссылки на
    фото рассылок ведут на домен остановленного дубликата-легаси. Все 5
    исправлены и задеплоены другой сессией, проверено без реальной
    рассылки живым пользователям (тестовый чат, поддельная
    Telegram-библиотека для админа заведения). Бэкапы:
    `*.bak-broadcast-20260923-104846` рядом с `admin.js`/`bot.js`/`richText.js`.
    Готовый сокращённый текст рассылки (636 видимых символов, Telegram
    Markdown) о фиче печати ТТК подготовлен этой сессией и передан
    пользователю — можно отправлять сегодняшнюю рассылку заново, в
    прошлый раз она не дошла ни до кого, дублей не будет.
    Follow-up, найденный по ходу проверки: среди адресатов "всем" есть
    тестовый чат из уже удалённого заведения (id 15) — на нём КАЖДАЯ
    рассылка "всем" будет показывать красную плашку "чат не найден — 1",
    даже если реальным адресатам всё доставлено. Пользователь выбрал
    системный фикс — фильтровать пользователей удалённых заведений из
    получателей рассылки everywhere (не точечно убирать tg_id у одного
    тестового чата). Задача поставлена другой сессии, ответ ещё не
    получен в разговоре.
38. **Реальный пропущенный заказ на моноблоке ОКО** — пришёл заказ с
    "Облако" (булочки), чек не вышел. Разбор с другой сессией: задание
    на печать создано корректно (`restaurantId: "1"`, нужная очередь —
    баг класса "p.36" здесь ни при чём, `oko-order-api.js` restaurantId
    ставит явно), но агент на моноблоке с 19:37 не мог авторизоваться —
    отвечал 401, слал токен `CHANGE_ME` (значение по умолчанию). Причина:
    `agent-config.json` на моноблоке сбросился — вероятно, старый баг в
    `loadConfig()`: при ЛЮБОЙ ошибке чтения файла (например, файл
    пересохранён в Блокноте) функция МОЛЧА перезаписывала его дефолтными
    значениями, включая `CHANGE_ME`. Точно доказать не удалось — старый
    файл затёрт, но совпадает по времени с моментом самообновления
    агента до версии `.8`.
    - Другая сессия сразу пофиксила причину (версия `2026-09-23.9`,
      уже на сервере): BOM теперь срезается при чтении, а если конфиг
      всё-таки не читается — агент НЕ перезаписывает его, а сохраняет
      копию `agent-config.json.broken-<время>` и оставляет оригинал.
    - Хотела временно разрешить `CHANGE_ME` с IP ОКО, чтобы чек вышел
      сразу, без похода к моноблоку — автопроверка безопасности это
      заблокировала без явного разрешения пользователя. Пользователь
      выбрал НЕ включать байпас (лишний риск для сети ОКО ради экономии
      одного похода к моноблоку) — просто физически заменить файл.
    - На месте вскрылся ВТОРОЙ, независимый слой путаницы: на рабочем
      столе оказались задвоенные файлы агента (отдельный `print-agent.js`
      прямо на Desktop, плюс полноценная папка `KD` с конфигом и
      ярлыками), и оба штатных способа запуска — двойной клик по
      `print-agent.js` (запускается через Windows Script Host, не
      Node — синтаксическая ошибка) и по `.vbs`-ярлыку автозапуска
      (ссылается на несуществующий старый путь, скорее всего с ДО
      переименования/переноса папки в "KD") — не работали. Даже прямой
      запуск через `KitchenDeskPrintAgentLauncher.bat` продолжал слать
      `CHANGE_ME` — похоже, его `cd /d` внутри тоже вёл в другое,
      стороннее место со своей старой копией файлов.
    - Решение — в реальном времени, по фото с моноблока (пользователь на
      сенсорном экране без физического Shift), провели через: открыть
      "Командная строка" через кнопку Пуск → `cd` вручную в правильную
      папку `C:\Users\of2\Desktop\KD` → `node print-agent.js` напрямую.
      Сработало: агент поднялся с верным токеном, тут же сам обновился
      до `.9`, и пропущенный заказ на булочки распечатался (с опозданием
      в несколько часов).
    - Заодно там же пересобрали автозапуск: `node print-agent.js
      --install-autostart` — теперь ярлыки корректно указывают на папку
      `KD`, так что после любой будущей перезагрузки моноблока агент
      поднимется сам, без повторения сегодняшней истории. Причина
      сегодняшнего сбоя (сброс токена) тоже пофикшена в `.9` — этот
      конкретный сценарий поломки закрыт с двух сторон: и код агента, и
      автозапуск на конкретном моноблоке.
    - Не выясненное до конца: откуда взялась вторая, "лишняя" копия
      файлов на рабочем столе и почему у ярлыков был устаревший путь —
      возможно, кто-то на месте (сотрудник ОКО) вручную копировал/
      переносил файлы в прошлом, не зная, что путь зашит в ярлыки
      намертво. Не критично сейчас, раз автозапуск пересобран заново, но
      стоит иметь в виду при следующей похожей истории на этом
      моноблоке — лишние копии файлов на рабочем столе могли остаться.

39. **2026-09-24 — первая тестовая сборка мобильного приложения.**
    Пользователь попросил собрать первую версию APK, вход временно по
    PIN-заглушке (2606), настоящую авторизацию (одноразовый код + PIN)
    решили сделать позже отдельным шагом — сейчас просто обёртка сайта.
    - Новая папка `oko-mobile-app/` — проект на Capacitor (WebView-
      обёртка), `.github/workflows/build-mobile-apk.yml` собирает APK
      через GitHub Actions (песочница агента не достаёт до серверов
      Android SDK, локально собрать нельзя — подтверждено).
    - `www/index.html` — весь "наш" код: экран с PIN, дальше редирект на
      сайт (сейчас `TARGET_URL = https://kitchendesk.chefplan.ru/web`).
    - Пройден полный цикл поломок при реальном тестировании на двух
      устройствах (планшет = суперадмин, телефон = главный админ ОКО):
      1. `getWorkflowRunLogsUrl`/скачивание артефакта напрямую —
         заблокировано тем же egress-прокси, что раньше блокировал
         Azure blob storage для print-agent. Решение — не пытаться
         скачивать самому, сразу давать пользователю ссылку на скачивание
         вручную (сработало и для скачивания APK-артефактов, и для
         логов упавших сборок).
      2. Сессия сайта не переживала закрытие приложения — прошли через
         несколько ЛОЖНЫХ гипотез (настройки батареи телефона, версия
         Android System WebView, явный `CookieManager.flush()` в
         `MainActivity.java`) прежде чем дойти до настоящей причины.
      3. Настоящая причина №1 (техническая, инфраструктурная): каждая
         сборка через GitHub Actions подписывалась СЛУЧАЙНЫМ debug-
         ключом (Android Gradle Plugin сам генерирует
         `~/.android/debug.keystore`, если его нет — а на свежем
         раннере его никогда нет). Установка APK с другим ключом поверх
         старого — для Android другое приложение, либо отказ в
         установке ("подписи не совпадают" — пользователь словил эту
         ошибку живьём), либо тихая потеря данных при вынужденном
         полном удалении-переустановке. Исправлено: сгенерирован
         постоянный `android/app/debug.keystore` (закреплён в
         репозитории, пароль "android" — не секрет, так и надо для
         debug), подключён через явный `signingConfigs.debug` в
         `app/build.gradle`. Только ОДИН раз потребовалось полное
         удаление старого приложения на обоих устройствах при переходе
         на этот постоянный ключ — дальше обновления ставятся поверх.
      4. Настоящая причина №2 (реальный баг): даже с постоянным ключом
         и чистой переустановкой сессия всё равно не сохранялась.
         Добавили диагностику — счётчик открытий в localStorage прямо
         на экране с PIN: он корректно рос (1→2→3) на обоих
         устройствах, значит хранилище WebView само по себе работает
         нормально. Другая сессия (с доступом к реальному коду сайта)
         нашла точную причину: `/web-login` при заходе ВСЕГДА рисует
         форму входа, не проверяя `localStorage.web_token` (JWT на 30
         дней) — а наша обёртка вела именно на `/web-login`, не на
         `/web` (который сам проверяет токен). Исправлено с двух
         сторон: обёртка перенаправлена на `/web`, и сама сессия
         `/web-login` (`oko-frontend/src/app/web-login/page.tsx`)
         теперь тоже сама проверяет токен и уводит на `/web` без формы,
         если вход уже есть (заодно поддержан `?redirect=` для будущих
         диплинков, например из Telegram-уведомлений на конкретный акт).
    - Заодно упростили раздачу тестовых сборок: сначала только через
      `actions/upload-artifact` (нужно идти в конкретный прогон, качать
      zip, распаковывать) — теперь ДОПОЛНИТЕЛЬНО публикуется в rolling
      GitHub Release (тег `mobile-test-latest`), прямая постоянная
      ссылка на `.apk`-файл, обновляется сама на каждой сборке:
      `https://github.com/alisherphoto10-png/kitchen-guide/releases/download/mobile-test-latest/kitchendesk.apk`.
    - Обсудили и сознательно ОТЛОЖИЛИ: баннер "доступна новая версия"
      внутри приложения — решили, что не стоит того (не убирает ручную
      установку, а инфраструктуру для проверки версии пришлось бы
      городить отдельно). Правильное решение на будущее — опубликовать
      в Google Play (закрытый тестовый канал, $25/300тыс. сум
      одноразово) — но у пользователя сейчас нет свободных денег на
      это, отложено до момента, когда приложение будет готово хотя бы
      процентов на 80% и сможет полностью заменить Telegram-бота для
      сотрудников.
    - Также нашли по ходу (не проблема мобильного приложения, а
      диагностический бонус): Telegram Mini App (`/cabinet`, им
      пользуются повар/сушеф/шеф) НЕ будет работать в этой тестовой
      обёртке в принципе — он специально проверяет криптографические
      данные, которые может дать только настоящий Telegram. Только
      "Главный администратор" может войти через `/web-login` в обычном
      WebView. Это ограничение уйдёт только вместе с настоящей
      авторизацией (одноразовый код + PIN), которую ещё предстоит
      реализовать.

40. **2026-09-24 (тот же день) — фирменный стиль, полноценный сценарий
    первого входа, редизайн по ТЗ.** Одна длинная сессия правок
    `oko-mobile-app/www/index.html`, только сторона обёртки (без
    доступа к серверу), несколько коммитов подряд:
    - Перенесена цветовая палитра и фирменный фон/лого из макета-демо
      (артефакт выше) в реальное приложение: синяя навигационная
      палитра вместо янтарной, настоящее фото кухни на фоне,
      лого KitchenDesk на заставке и в кольце поиска (меняется на лого
      заведения при находке).
    - Приложение стало реально запоминать на устройстве, что вход уже
      пройден (`localStorage`), и при повторном открытии сразу
      показывает быстрый PIN, минуя код/поиск/находку. Добавлена
      кнопка "Сбросить и посмотреть первый вход заново" на этом
      быстром PIN-экране — только для тестирования.
    - Плавность экрана находки заведения доработана (обложка/подложка
      плавно проявляются вместо рывка, лого заведения крупнее и по
      центру, карточка с названием "въезжает" снизу).
    - Добавлены заглушки полной регистрации: главный экран (вместо
      сразу кода), форма имя+телефон+код, шаг согласия с документами
      (черновой типовой текст политики конфиденциальности и оферты,
      нужна проверка юристом), запрет скриншотов `FLAG_SECURE`
      (сейчас временно закомментирован — пользователь ещё тестирует и
      сам делает скриншоты), заглушка входа по логину/паролю (для
      случая смены телефона).
    - **Пользователь прислал полноценное ТЗ на редизайн** (файл
      `KitchenDesk_TZ_redesign_first_launch-2.docx` + референс-макет
      10 экранов) с точной дизайн-системой "Industrial Digital":
      `#070B12` фон, `#4D7FFF` акцент, Inter, левое выравнивание вместо
      центрирования, поля/кнопки с точными размерами (без capsule-формы),
      панель "← Назад / 0X/05" с прогрессом на внутренних экранах,
      переходы fade+translateX. Приложение полностью пересобрано под
      это ТЗ: экран "Вход" по логину/паролю стал полноценным (не
      заглушкой-ссылкой), карточка заведения с ролью подключения на
      экране находки, честный цикл создания PIN — придумать → повторить
      → "Готово!" (раньше PIN сразу вёл на сайт без подтверждения).
      PIN, который человек сам вводит при регистрации, теперь и
      реально проверяется при возврате (хранится вместе с заведением в
      `localStorage`), а не тестовый захардкоженный код. Убрана
      техническая плашка "демо-сценарий входа..." из интерфейса.
    - По итогам живого тестирования на телефоне поправлено: кнопки
      главного экрана были слишком прижаты к низу — подняты выше;
      галочка на экране "Готово!" не отображалась (SVG не наследовал
      `stroke`/`fill` от общего класса иконок) — исправлено; убран
      декоративный шеврон у "Сотрудник кухни"; поле телефона получило
      отдельный префикс кода страны `+998` и автоформатирование по
      маске "90 927 35 25" вместо сплошной строки цифр (и на входе, и
      при регистрации).
    - **Последнее по времени:** обложка заведения (фото с экрана
      находки) больше не сбрасывается на общий фон кухни на
      документах/PIN/"Готово!" — держится тем же фоном до самого конца
      сценария. При нажатии "Перейти к входу" добавлен короткий экран
      перехода: обложка сворачивается из полноэкранной в баннер сверху
      (~900ms), внизу проявляется "Открываем KitchenDesk…", и только
      потом происходит `window.location.href` на сайт. Это красиво
      только В ОБЁРТКЕ — реальная бесшовность (чтобы этот же баннер
      оставался и на дашборде после перехода) требует работы на
      стороне сайта, см. задачу ниже.

## Открытые вопросы / что в работе (актуально на 2026-09-24)

- **Диагностика этикеточного принтера (USB, TSPL)** — НЕ ЗАВЕРШЕНА, тема
  оставлена на середине (см. п.33). Канал печати подтверждён рабочим
  (текст печатается), но последний тест (`2026-09-23.7`, плотные строки
  "X" тем же шрифтом, что раньше дал чистый результат) показал по
  описанию пользователя "половина буквы по всей строке" — фото НЕ
  прислано, разбор не завершён. Нужно: попросить пользователя прислать
  фото именно этого теста (версия `2026-09-23.7`), посмотреть — обрезана
  ли одинаково ВЕРХНЯЯ/НИЖНЯЯ половина у каждой буквы на ВСЕХ строках
  (похоже на настройку высоты строки/шрифта, не на поломку) или пропуск
  на одном и том же месте ПО ШИРИНЕ на каждой строке (это уже физический
  дефект головки, тогда пользователь склоняется к ремонту). До этого
  разбора выводов о необходимости ремонта делать не стоит — есть
  весомый контраргумент: обычный `TEXT` то же самое время печатал
  строку "TEST 58x30mm" чисто и стабильно много раз, что плохо
  сочетается с версией "голова физически сломана".
- **Печать ТТК на чековом принтере** — ГОТОВО И ПОДТВЕРЖДЕНО В БОЮ (см.
  п.34-36): повар Камиль дважды напечатал ТТК вживую, агент отметил
  задания выполненными. Оба попутно найденных бага (чек-лист смены не
  печатался, тире в тикетах — "?") исправлены и проверены. Ширина ленты
  исправлена с ошибочных 58мм/32 симв. на реальные 80мм/42 симв.
  (`AGENT_VERSION` агента → `2026-09-23.8`, `WIDTH` в `ttkPrint.js` →
  `42`). Единственное, что осталось по этой теме: **не проверено, не
  48 ли реально символов** (взяли 42 осторожно, т.к. точное число точек
  головки принтера неизвестно без замера) — при следующей реальной
  печати ТТК можно посмотреть, доходит ли разделитель `=====` до
  правого края ленты; если нет — почти наверняка 48, можно поднять
  `WIDTH`. Не срочно, 42 работает корректно.
- **Рассылка (суперадмин + админ заведения)** — 5 найденных проблем
  исправлены (см. п.37): длинный текст с фото больше не падает, есть
  счётчик символов, сервер возвращает причину ошибки, ссылки на фото
  ведут на актуальный домен. **Остался один follow-up**: отфильтровать
  пользователей удалённых заведений из получателей ЛЮБОЙ рассылки
  "всем" (сейчас тестовый чат удалённого заведения 15 будет вызывать
  красную плашку "чат не найден — 1" почти на каждой такой рассылке,
  даже если реальным адресатам всё доставлено) — задача поставлена
  другой сессии, ответ ещё не получен.
- **Доработка OKO-модулей под несколько заведений** — приоритет,
  который пользователь сам назвал текущим ~неделю назад (~1-2 недели на
  это). По факту сегодня (2026-09-24) в разговоре занимались в основном
  мобильным приложением, а не этим — работа по мультизаведенческой
  доработке модулей (пособие официанта, инвентаризация утвари, заказы
  Облако/Мясо, см. п.21) так и не начата. Стоит спросить пользователя,
  актуален ли ещё этот порядок, раз фактическая работа пошла по-другому.
- **Мобильное приложение** — редизайн сценария первого входа готов и
  протестирован живьём (см. п.40): полный путь главный экран → вход/
  регистрация → находка заведения → документы → создание PIN → "Готово!"
  → переход на сайт, оформлено по присланному ТЗ "Industrial Digital".
  Вход по-прежнему НЕ настоящий (нет бэкенда): код приглашения принимает
  любое значение, находит один и тот же тестовый ОКО, вход по
  логину/паролю — заглушка с сообщением "скоро заработает". PIN,
  который человек сам придумывает, реально сохраняется и проверяется
  на этом устройстве (localStorage), но это не серверная авторизация.
  `FLAG_SECURE` (запрет скриншотов) реализован, но сейчас закомментирован
  по просьбе пользователя — включить обратно перед реальным
  использованием. Google Play отложен до ~80% готовности функций и до
  появления денег на аккаунт разработчика ($25/300тыс.сум).
- **Баннер заведения на дашборде (`/web`) — ГОТОВО (2026-09-24).**
  Поставлено другой сессии тем же днём, реализовано и подтверждено
  скриншотами с телефона и компьютера. Что сделано: у заведений в базе
  раньше не было поля под фото — добавили (обложка + лого, по
  умолчанию пустые, поэтому у остальных заведений ничего не изменилось,
  пока сами не загрузят). Для ОКО сразу поставили те же фото, что
  зашиты в мобильной обёртке (фон зала и лого из пособия официанта) —
  при переходе из приложения на сайте виден тот же кадр. Загрузка — в
  "Управление → Настройки → Заведение" (превью + Загрузить/Заменить/
  Убрать), править может админ/шеф/суперадмин, су-шеф и повар только
  видят баннер. Показывается на всех разделах, КРОМЕ "Управления", как
  и просили. На телефоне — полоса 140px с тем же тёмным градиентом,
  что в обёртке; на компьютере — закруглённая карточка над контентом;
  лого+название мелко внизу слева; боковое меню и шапка не менялись
  (бренд KitchenDesk остаётся главным); уезжает вверх при прокрутке;
  фото кешируется на телефоне, так что при повторных входах баннер
  виден сразу, без мигания пустого места. Резервные копии изменённых
  файлов — с пометкой `bak-venuebanner` рядом с оригиналами.
  **Осталось на стороне обёртки** (не сделано, отдельная задача на
  будущее): обёртка до сих пор не делает реальный поиск заведения по
  коду приглашения, только показывает захардкоженное фото ОКО и ничего
  не спрашивает у сервера. Настоящая бесшовность для ДРУГИХ заведений
  появится только когда в обёртке будет реализован настоящий поиск —
  и тогда сервер должен в ответе на поиск по коду отдавать обложку и
  лого заведения (сейчас их можно получить только после входа, не на
  этапе поиска).
- **Проект «Жигули» (отдельный от KitchenDesk продукт калькуляций/ТТК)
  — ШАГ 1 ГОТОВ И РАЗВЁРНУТ (2026-09-24).** Кодовое имя, чтобы не
  путать с основным продуктом. Реальный платный заказ, ведётся
  полностью изолированно: `/root/zhiguli` на сервере, своя база
  `zhiguli`, свой процесс pm2 `zhiguli` (порт 3008), код в отдельной
  ветке `zhiguli` в этом же репозитории (`kitchen-guide`) — общей
  истории с KitchenDesk нет, папки/база основного продукта не
  затронуты. **Подробности, устройство, команды и план шага 2 (боты)
  — в отдельном `README.md` в корне ветки `zhiguli`, не здесь** (это
  сознательное разделение, как и просил пользователь — не плодить
  детали чужого проекта в координационном файле KitchenDesk).
  Коротко для контекста: сайт называется в интерфейсе «Калькуляции»,
  светлый "бумажный" стиль с терракотовым акцентом (нарочно не похож
  на тёмный KitchenDesk), роли владелец/технолог/повар, калькулятор в
  4 режимах с выгрузкой в PDF/Excel (собираются на сервере — задел под
  будущего бота), импорт из iiko, 45 API-тестов пройдено. Публично
  доступен: **https://jigulibar.chefplan.ru** (Let's Encrypt,
  автопродление). Вход платформы: `alisher / t5v2zyfgzjyy` (пользователь
  предупреждён сменить). Демо-заведение «Демо-кухня» с 8 ТТК для показов
  клиентам. Новых заказчиков заводить через «Заведения» → «+ Заведение».
  **Шаг 2 (Telegram-боты) — ГОТОВ И ЗАДЕПЛОЕН, НО ЕЩЁ НЕ ПРОВЕРЕН С
  НАСТОЯЩИМ БОТОМ (2026-09-24)** — все 45 сценариев и ручной путь
  прогнаны на подменённом Telegram, реального токена у сессии не было.
  **Нужна живая проверка от пользователя:** завести тестового бота в
  @BotFather → подключить его "Демо-кухне" (кнопка "Подключить бота" в
  "Заведения", или сразу при создании заведения) → в "Команде"
  отправить приглашение "В Telegram" для demo.cook себе → открыть
  мини-апп через кнопку бота "ТТК". Если что-то не так — присылать,
  что показывает панель бота (статус вебхука, ошибки доставки).
  Коротко как устроено: токен хранится зашифрованным (ключ в
  `server/.env` на сервере — **важно сохранить его отдельно**, при
  потере все токены ботов вводить заново), у каждого заведения свой
  вебхук с секретом, сотрудник привязывается к боту заведения
  разовой ссылкой (24 часа) через "Команда" → "В Telegram", вход в
  мини-апп потом без пароля. Известное ограничение: скачивание
  PDF/Excel изнутри Telegram-мини-аппа может не работать (плохая
  поддержка скачивания во встроенном браузере Telegram) — выгрузку
  лучше делать с сайта; повару выгрузка и так недоступна. Подробности
  — в README ветки `zhiguli`, не переносить сюда.
- **Групповая привязка сотрудников по логину/паролю в «Жигулях» —
  ГОТОВО И ЗАДЕПЛОЕНО (2026-09-24), настоящим ботом всё ещё не
  проверено** (только подменённый Telegram API, как и шаг 2). Заменила
  персональные одноразовые ссылки полностью — их таблица в базе
  удалена, ничего не затронуто (реальных ботов ещё не подключали).
  Как устроено: при добавлении сотрудника в "Команде" владелец сразу
  получает логин + случайный пароль (можно заменить своим от 6
  символов или перегенерировать) — этими же данными сотрудник входит
  и на сайт. Вверху "Команды" — карточка "Ссылка на бота для всех
  сотрудников" с копированием, кидается в общий чат. Первый /start →
  бот спрашивает логин, потом пароль (сообщение с паролем сразу
  удаляется из чата; /cancel сбрасывает ввод) → при совпадении с ещё
  не привязанным сотрудником ЭТОГО заведения — Telegram закрепляется
  навсегда, дальше без пароля. Уже привязанный логин — отказ с
  просьбой обратиться к владельцу, без подсказки что именно не так.
  5 неверных попыток подряд — блокировка на 15 минут для этого
  Telegram. Владелец видит в "Команде" `Telegram привязан · @username`
  и может сбросить привязку для повторной. В групповых чатах бот
  логин не спрашивает, молчит. 29 сценариев прогнаны (регистр логина,
  попытка занять чужого сотрудника, изоляция между заведениями,
  блокировка, сброс+повтор, отключённый сотрудник) + ручная проверка
  в браузере. **Живая проверка с настоящим ботом всё ещё не сделана** —
  пользователю предложено подключить тестового бота к "Демо-кухне" и
  войти как `demo.cook / b32w62pds6`.
- **Задача для «Жигулей» — правки профиля в мини-аппе + модули заведения
  (платное включение "Пересчёта") + тикет-система техподдержки —
  СДЕЛАНО и задеплоено 2026-09-24** (ветка `zhiguli`, коммиты `206712c`
  и `f9470a0`; подробности — раздел «Шаг 3» в README ветки `zhiguli`).
  Итог: профиль мини-аппа без пароля/выхода, с «Техподдержкой» и
  «Историей обращений», назад — «К ТТК», логотип и системная кнопка
  Telegram; модули — «Заведения» → «Модули» (реестр в коде, «Пересчёт»
  выключен по умолчанию, выключенный виден с замком); тикеты — ровно по
  сценарию ниже, инбокс «Поддержка» на сайте (только администратор
  платформы), уведомления — через ОДИН отдельный служебный бот (токен
  и группа/тема задаются в «Поддержка» → «Уведомления», группа/тема
  находится кнопкой «Найти чаты» после `/start@бот` в нужной теме).
  Дополнительно: команда `/support` в боте заведения делает то же, что
  кнопка. Проверено на фейковом Bot API + Playwright. **Не сделано живьём:**
  служебный бот ещё не создан (нужен токен от пользователя из
  @BotFather), живым ботом заведения сценарий не прогоняли.
  Исходная постановка (для истории):
  **1. Профиль в мини-аппе (со стороны повара, живая проверка через
  подменённый профиль показала проблемы):**
  - Убрать пункт "Сменить пароль" — не нужен там.
  - Убрать кнопку "Выйти" — тоже не нужна.
  - Добавить пункт "Техподдержка" — ведёт на простую форму отправки
    сообщения (см. п.3 ниже про инбокс, это не заглушка, а реальная
    связь сразу).
  - **Баг:** из профиля нет пути назад к калькуляциям — приходится
    полностью закрывать мини-апп и открывать заново. Нужна нормальная
    кнопка/переход "назад" с профиля на главный экран калькуляций.
  **2. Платное включение функции "Пересчёт" (архитектурная задача, не
  разовый хак) — обсудили с пользователем, вот согласованный подход:**
  Ввести общий механизм модулей/функций на уровне заведения — не
  хардкодить только под "Пересчёт", а сделать так, чтобы так же можно
  было продавать/включать другие функции в будущем, не переделывая
  систему заново. У каждого заведения в "Заведения" — список модулей
  с переключателем вкл/выкл (владелец/платформенный админ решает).
  По умолчанию для новых заведений — выключено, пользователь включает
  вручную, когда заведение доплатило. Первый модуль — "Пересчёт"
  (калькулятор из 4 режимов, который уже есть). Если выключен: сама
  функция калькулятора и на сайте, и в боте остаётся ВИДНА, но
  заблокирована — с понятным текстом ("Доступно в расширенном тарифе,
  обратитесь к администратору"), не просто молча не работает. Базовые
  калькуляции (карточки ТТК, поиск, состав) этот переключатель не
  затрагивает — работают всегда независимо от модулей.
  **3. Техподдержка — полноценная тикет-система (финальная версия,
  уточнена пользователем 2026-09-24, отменяет более раннюю простую
  версию "написал → ответил").** Видимость истории для владельца
  заведения — пользователь явно сказал НЕ делать, не нужно.
  **Сценарий целиком:**
  1. Сотрудник в профиле мини-аппа нажимает "Техподдержка" (можно
     назвать и "Написать нам") → мини-апп закрывается, в обычном чате
     с ботом приходит сообщение "Напишите своё обращение и отправьте".
  2. Сотрудник пишет текст обычным сообщением в чат — этим создаётся
     новый тикет (обращение), привязанный к заведению и отправителю.
  3. Тикет появляется в инбоксе на сайте (список обращений по всем
     заведениям) И одновременно уведомление о новом тикете уходит в
     Telegram-группу пользователя.
     **Важное уточнение (2026-09-24, пользователь сам заметил проблему
     до того, как это успели построить неправильно):** уведомление
     должно слать НЕ бот того заведения, от которого пришёл тикет —
     иначе пользователю пришлось бы вручную добавлять КАЖДЫЙ бот
     каждого заведения в свою группу, что неудобно и не масштабируется.
     Вместо этого — **отдельный служебный бот** (один-единственный,
     не привязанный ни к одному заведению), созданный и добавленный в
     группу пользователя ОДИН раз навсегда. Сервер при создании тикета
     от любого заведения шлёт уведомление через ЭТОТ служебный бот, а
     не через бот заведения-источника, указывая в тексте сообщения имя
     заведения. При добавлении новых заведений в группу ничего
     донастраивать не нужно. Адрес группы (chat_id) для этого
     служебного бота должен быть настраиваемым.
  4. Админ отвечает текстом на сайте → ответ уходит сотруднику в тот
     же чат с ботом (`sendMessage` на его telegram_id).
  5. Пока тикет открыт — любое следующее сообщение от этого
     сотрудника боту добавляется в переписку ЭТОГО ЖЕ тикета, а не
     создаёт новый. Переписка идёт туда-сюда, пока вопрос не решён.
  6. Когда вопрос закрыт — админ закрывает тикет на сайте вручную.
  7. Следующее обращение сотрудника (новый вопрос, в другой раз) —
     тот же сценарий с п.1, создаётся уже НОВЫЙ тикет.
  8. **Важно:** вся история переписки (все тикеты, открытые и
     закрытые) должна быть доступна сотруднику для просмотра ПРЯМО В
     МИНИ-АППЕ (раздел вроде "История обращений" в профиле) — не
     только живьём в чате Telegram, а именно как отдельный
     просматриваемый список/история внутри приложения.
- **Несколько «владельцев» на одно заведение — ОТВЕТ ПОЛУЧЕН, доработка
  НЕ НУЖНА (2026-09-24).** Система уже поддерживает несколько записей
  с ролью "владелец" в "Команде" одного заведения — не ограничено
  одним. Пользователь этим и пользуется теперь вместо роли "технолог":
  назначает полный доступ нескольким людям через "владелец" (например,
  Алексей и Фируза — оба "владелец" одного заведения). Роль "технолог"
  пользователь у себя удалил (не использует).
- **Задача для «Жигулей» — экспорт файлов в чат бота + бренд KitchenDesk
  на документах + свой пароль сотруднику + редактируемая карточка тикета —
  СДЕЛАНО и задеплоено 2026-09-24** (ветка `zhiguli`, коммит `ef44a9b`,
  раздел «Шаг 4» в README ветки `zhiguli`). Итог: из мини-аппа PDF/Excel
  приходят документом в чат с ботом заведения, на сайте — скачивание как
  было; «Калькуляции — модуль KitchenDesk» мелко в подвале PDF и в конце/
  колонтитуле Excel; «Команда» → «Пароль» — можно вписать свой (от 6
  символов, одни цифры подходят; раньше было только автогенерирование);
  уведомление о тикете — одно сообщение, редактируется, статус сверху
  (🟠/🔵/✅ ЗАКРЫТО). Учесть: правка сообщения в Telegram беззвучна.
  Роль «технолог» в «Жигулях» убрана полностью (коммит `a4d53b3`).
  **Баг с дублирующимся сообщением после экспорта — ИСПРАВЛЕНО
  (2026-09-24, коммит `246c626` в `zhiguli`), но настоящим ботом
  повторно не проверено (только поддельный Telegram, 8 сценариев).**
  Причина: Telegram при выгрузке файла из мини-аппа шлёт боту
  служебное уведомление без содержимого, и бот отвечал приветствием с
  кнопкой "Открыть ТТК" на ВСЁ, что приходит, включая эти служебные
  уведомления — отсюда дубль после каждого файла. Теперь бот отвечает
  только на то, что человек реально написал/прислал (текст, команды,
  фото, файлы, стикеры); служебные сообщения без содержимого молча
  пропускаются и только логируются (тип, без содержимого) — так что
  при следующей живой выгрузке будет видно по логу, что именно
  присылает Telegram в этот момент. Заодно то же самое исправлено и
  для непривязанных пользователей — служебные уведомления больше не
  триггерят у них просьбу ввести логин.
  **Нужна повторная живая проверка** — сделать ещё один экспорт в чат
  из мини-аппа настоящим ботом и убедиться, что лишнего сообщения
  больше нет.
  Исходная постановка (для истории):
  **1. Экспорт PDF/Excel — по каналу запроса (уточнено пользователем).**
  На САЙТЕ экспорт остаётся как есть — обычное скачивание в браузере,
  не трогать. В БОТЕ/мини-аппе — известное ограничение (уже записано
  выше в этом файле): скачивание внутри Telegram-мини-аппа плохо
  работает из-за встроенного браузера. Решение: когда экспорт
  технологической карты в PDF/Excel запрашивают ИЗ БОТА, бот должен
  прислать готовый файл сообщением в тот же чат, откуда пришёл запрос
  (`sendDocument`), а не пытаться отдать на скачивание через браузер
  мини-аппа. Иначе говоря: источник запроса определяет способ доставки
  — сайт всегда браузером, бот всегда чатом.
  **2. Лёгкий бренд KitchenDesk на самих файлах экспорта.** Пользователь
  хочет, чтобы на экспортируемых PDF/Excel-файлах (технологические
  карты) была ненавязчивая отметка KitchenDesk — не реклама в лоб, а
  скорее позиционирование "«Калькуляции» — модуль KitchenDesk" (у
  пользователя KitchenDesk — основной продукт, «Жигули»/«Калькуляции»
  он воспринимает как его модуль/дочерний продукт, и хочет мягкое
  упоминание бренда на документах, которые потом ходят по рукам на
  кухне/у поставщиков — по сути пассивная реклама основного продукта).
  Конкретное визуальное решение (небольшая подпись в подвале файла,
  маленький логотип и т.п.) — на усмотрение сессии, главное — ненавязчиво,
  не портить читаемость самой технологической карты. **Важно не
  путать со стилем интерфейса** — сам сайт/бот "Жигулей" сознательно
  визуально НЕ похож на KitchenDesk (решение записано выше), это
  касается только отметки на экспортируемых ДОКУМЕНТАХ, не интерфейса.
  **3. Пароль сотрудника — проверить/обеспечить возможность задать
  простой пароль вручную.** Пользователь пробовал сгенерировать новый
  пароль кнопкой — получает сложный (буквы+цифры), неудобно для
  сотрудников кухни. По более раннему отчёту в системе уже должна быть
  возможность вписать СВОЙ пароль (от 6 символов), не только
  генерировать случайный — нужно перепроверить, что это реально
  работает и заметно в интерфейсе "Команды" (возможно, проблема просто
  в том, что пользователь не увидел/не нашёл это поле, а не в том, что
  функции нет). Цель: после первого автогенерируемого пароля владелец
  должен суметь заменить его на простой (например, только цифры) для
  удобства сотрудника.
  **4. Уведомление о тикете в группе — редактировать сообщение, не
  плодить новые.** Сейчас (по уже сделанной тикет-системе) непонятно,
  шлёт ли повторное сообщение при каждом новом сообщении в переписке —
  пользователь явно попросил: НЕТ, не слать новое сообщение на каждое
  дополнение переписки. Вместо этого при создании тикета бот шлёт ОДНО
  уведомление в группу, и дальше, пока тикет открыт, это же сообщение
  РЕДАКТИРУЕТСЯ (`editMessageText`) по мере переписки — чтобы, зайдя в
  тему, пользователь не путался среди кучи отдельных сообщений по
  одному и тому же обращению. Когда админ закрывает тикет на сайте —
  это же сообщение в группе тоже должно измениться, явно показывая,
  что обращение закрыто (текстом/пометкой/эмодзи — на усмотрение
  сессии, лишь бы было сразу видно статус, не открывая сайт).
- **Баг в «Жигулях» — пункты меню «Заведения»/«Поддержка» ошибочно
  видны внутри заведения — ИСПРАВЛЕНО В КОДЕ (2026-09-25, ветка
  `zhiguli`, коммит `2774a51`); задеплоено 2026-09-24 вместе с шагом 5
  (сборка веба из ветки после rebase).**
  Пользователь заметил живьём: заходит как администратор платформы
  внутрь конкретного заведения — а в меню (десктоп-сайдбар и нижние
  вкладки на мобильном) всё равно остаются «Заведения» и «Поддержка»,
  хотя они должны быть только на уровне платформы (до входа в
  заведение). Причина — в `web/src/components/Layout.tsx`, функция
  `useNav()`: пункты добавлялись по одному условию
  `me?.user.is_platform_admin`, без проверки `!me?.tenant` (то есть без
  проверки, что админ ещё не зашёл внутрь заведения). Баннер «Вы внутри
  заведения... как администратор платформы» на той же странице уже
  считает правильно (`is_platform_admin && me.tenant`) — теперь и меню
  использует симметричное условие `is_platform_admin && !me?.tenant`.
  Заодно опрос бейджа непрочитанных тикетов (`useSupportWaiting`)
  включён тем же условием — не дёргает `/platform/support/summary`,
  пока админ внутри заведения (там бейдж всё равно не показывается).
  Проверено: `tsc -b --noEmit` в `web/` проходит чисто. **Нужно от
  сессии с доступом к серверу:** `git pull` ветки `zhiguli` в
  `/root/zhiguli`, пересобрать веб (`npm run build` в `web/`) и
  перезапустить/передеплоить как обычно — сама сессия без доступа к
  серверу задеплоить это не может.
- **«Жигули», шаг 5 — ссылка на гид для владельцев «Как это работает» —
  СДЕЛАНО и задеплоено 2026-09-24** (ветка `zhiguli`, коммит `ce319c2`,
  раздел «Шаг 5» в README ветки `zhiguli`). Ссылка хранится в одном
  месте — настройка платформы «Поддержка» → «Гид» (по умолчанию
  `https://claude.ai/artifact/2XkkoVWcDHjLzmjLEsokvk`; пустое поле скрывает
  гид везде). Где видно: страница входа («Что такое «Калькуляции»?»),
  боковое меню и профиль сайта, профиль мини-аппа (открывается в
  браузере через `openLink`), команда `/guide` в меню каждого бота
  заведения (одно сообщение с кнопкой-ссылкой; прочие команды бота не
  затираются). `/start` и автоответы не менялись — дубль сообщений не
  вернётся. У живого @DEMOKUXNYAbot `/guide` в меню появился при старте
  сервера; нажатие `/guide` настоящим ботом — за пользователем.
- **Гид — перенос со страницы claude.ai на собственный домен —
  СДЕЛАНО И ЗАДЕПЛОЕНО.** Живёт по `https://jigulibar.chefplan.ru/guide/`
  (ветка `zhiguli`, статика в `web/public/guide/`, отдаётся отдельным
  `express.static('/guide', ...)` в `server/src/index.js`, коммит
  `6be07e6` — раньше `/guide/` без этого отдавал SPA, у общей статики
  выключен автоматический `index.html`). Ссылка в «Поддержка» → «Гид»
  переключена на этот адрес, подхватывается везде: страница входа, меню
  и профиль сайта, профиль мини-аппа, ответ бота на `/guide`.
  **Дефект с наложением карточки на кнопку на телефоне (360–390 px) —
  ИСПРАВЛЕН (коммит `8d98dee`)**: на мобильной раскладке `.hero .wrap`
  наследовал `align-items: center` от десктопной версии, трек строки с
  фото-карточкой был меньше её содержимого, и центрирование тянуло
  карточку вверх, на кнопку. Добавлен `align-items: start` в мобильном
  медиа-запросе.
  **Эффект "карточки наслаиваются одна на другую" — УБРАН ПО ПРОСЬБЕ
  ПОЛЬЗОВАТЕЛЯ (2026-09-25, коммит `e90e3a3`).** Пользователь смотрел
  живьём — решил вернуть страницу к исходному виду: один сплошной
  длинный скролл, без `position: sticky`/JS-расчёта прогресса (тот
  механизм несколько раз давал глюки на реальных устройствах — с
  этим боролись в предыдущих версиях, проще было убрать совсем).
  Убраны: `.pin`/`.layer` (sticky-рельсы, скругления и тень "карточки",
  полноэкранные фоновые фото каждого раздела — `bg-herbs.jpg` и т.п.,
  сами файлы остались в `web/public/guide/img/`, просто больше не
  используются), JS-блок расчёта scroll-прогресса. Добавлена тонкая
  линия-разделитель (`border-top`) между блоками — простое визуальное
  разделение взамен сложной механики. Не тронуто и работает как
  раньше: анимация появления при скролле, интерактивные демо (поиск,
  переход в карту ПФ, калькулятор, импорт из iiko, смена пароля, чат
  бота), парящая карточка в шапке, тёмная закрывающая секция «Остались
  вопросы?» (сейчас просто тёмный фон без фото кухни — фото тоже было
  частью убранного эффекта), блок FAQ и слоты `data-guide-slot` под
  редактируемые фото (см. следующий пункт).
  **Нужно от сессии с доступом к серверу:** `git pull` ветки `zhiguli`,
  пересобрать веб (`npm run build` в `web/`), передеплоить — правки
  только в статическом файле `web/public/guide/index.html`, серверный
  код и так уже раздаёт `/guide/` правильно, трогать не нужно.
  **ЗАДЕПЛОЕНО 2026-09-24 (`e90e3a3`) и проверено в Playwright** на 1280,
  1920, 390 и 360 px: лента сплошная, `sticky` только у шапки, все 9
  секций проявляются при прокрутке, кнопка «Посмотреть, как это работает»
  ничем не закрыта, горизонтальной прокрутки нет, все картинки грузятся,
  ошибок JS нет. **Найдены 2 дефекта в `index.html` (не правил — файл
  ведёт другая сессия):**
  1. «Остались вопросы?» — белый текст на СВЕТЛОМ фоне, почти не виден:
     общее правило `.hero, .feature, #faq, .final { background: var(--bg) }`
     стоит ниже `.final { background: #1D2A1F }` и перебивает его. Исправить:
     убрать `.final` из того списка (или поднять специфичность, например
     `section.final`).
  2. Пока `/api/guide/content` отвечает 404, блок «Частые вопросы»
     показывается пустым (только заголовок): при `!data` скрипт делает
     `return` раньше, чем прячет `#faq`. Исправить: при `!data` тоже
     `document.getElementById('faq').style.display = 'none'` (или скрыть
     блок по умолчанию и показывать только при непустом `faq`).
  **Оба дефекта ИСПРАВЛЕНЫ (2026-09-25, коммит `0d4eb3f` в `zhiguli`).**
  1. `.final` убран из общего списка `.hero, .feature, #faq { background:
     var(--bg) }`; `position: relative` (единственное, что оттуда ещё было
     нужно `.final`) перенесён в его собственный блок `.final { ... }`.
  2. В общий `hideFaq()` теперь заходят все ветки: и `if (!data) { hideFaq();
     return; }`, и `.catch(hideFaq)`, и случай пустого `data.faq`. Проверено
     в Playwright на 1280×900: при 404 на `/api/guide/content` — `#faq`
     скрыт, фон `.final` остаётся тёмным (`rgb(29,42,31)`); с подставленными
     `{ photos:{}, faq:[{question,answer}] }` — блок виден, вопрос
     рендерится, фон `.final` тот же тёмный. **Нужно от сессии с доступом
     к серверу:** `git pull` ветки `zhiguli`, пересобрать веб, передеплоить.
  **ЗАДЕПЛОЕНО 2026-09-24 (`0d4eb3f`) и проверено на проде** (Playwright,
  1280/390/360 px): «Остались вопросы? Пишите.» — белый текст на тёмном
  фоне `rgb(29,42,31)`; блок «Частые вопросы» полностью скрыт
  (`display: none`, текста на странице нет); закреплена по-прежнему только
  шапка, горизонтальной прокрутки нет, все картинки грузятся, ошибок JS нет.
- **Гид — редактируемые фото и FAQ из админки платформы — СДЕЛАНО И
  ЗАДЕПЛОЕНО 2026-09-24** (ветка `zhiguli`, коммит `7572bcb`, раздел «Шаг 6»
  в README ветки `zhiguli`). Сделано по плану ниже: миграция 008
  (`guide_photos`, `guide_faq`), публичный `GET /api/guide/content`
  (no-cache), админские `/api/platform/guide/photos/:slot` (POST/DELETE) и
  `/faq` (CRUD + `POST /faq/:id/move` для порядка). Окно «Поддержка» → «Гид»:
  ссылка, сетка 8 фото с подписями («Загрузить/Заменить», «↺» — вернуть
  заглушку; «без фона» — там лучше PNG с прозрачным фоном), список вопросов.
  Проверено на тестовом экземпляре (35 API + 12 Playwright): фото и вопросы
  появляются на `/guide/` сразу после сохранения, без сборки; HTML в вопросах
  показывается как текст. Попутно: ссылка по умолчанию в коде теперь
  `<PUBLIC_BASE_URL>/guide/`; в `index.html` гида добавлен только
  `white-space: pre-line` для ответов (иначе абзацы склеивались). На проде
  сейчас фото и вопросов нет — FAQ скрыт, заглушки на месте; заполнять —
  администратору платформы. **Новый `data-guide-slot` на странице =
  добавить его и в `SLOTS` в `server/src/services/guideContent.js`.**
  Исходная постановка (для истории): Пользователь
  хочет заходить в «Поддержка» (или рядом) как администратор платформы
  и: (1) заменять фото-заглушки блюд на странице гида на свои, (2)
  вести список «Частые вопросы», который сразу виден всем, кто
  открывает гид по ссылке — без переиздания страницы вручную.
  Фронтенд гида под это уже подготовлен (`web/public/guide/index.html`,
  актуален на коммит `e90e3a3` в `zhiguli`): 8 фото помечены
  `data-guide-slot="..."` (`hero-caesar`, `hero-cake`, `hero-carbonara`,
  `list-caesar`, `list-tomyum`, `list-carbonara`, `final-salmon`,
  `final-tomyum` — только блюда-заглушки, логотип KitchenDesk не
  трогать), и есть пустой блок `<section id="faq">` со списком
  `#faq-list`. При загрузке страница сама делает
  `fetch('/api/guide/content')` и, если получает
  `{ photos: {slot: url, ...}, faq: [{question, answer}, ...] }`,
  подменяет `src` у помеченных `<img>` и рисует аккордеон вопросов;
  если эндпоинта ещё нет — тихо остаётся как есть, ничего не ломает.
  Осталось сделать бэкенд и админку. По образцу уже существующих
  паттернов в коде (`server/src/routes/recipes.js` — загрузка фото
  через `multer` в `config.uploadsDir`, отдача через `/uploads`;
  `server/src/services/guide.js` + `requirePlatformAdmin` — как уже
  устроена настройка «Поддержка» → «Гид»):
  1. Миграция `008_guide_content.sql`: таблица `guide_photos` (`slot`
     TEXT PRIMARY KEY, `url` TEXT, `updated_at`) и `guide_faq` (`id`
     SERIAL, `question` TEXT, `answer` TEXT, `sort_order` INT,
     `created_at`).
  2. `GET /api/guide/content` — публично, без авторизации (страница
     гида открыта не только вошедшим): отдаёт только заполненные
     `photos` (slot→url) и весь `faq` по `sort_order`.
  3. Админские роуты под `/api/platform/guide/...`
     (`requirePlatformAdmin`, уже есть в `platform.js`):
     - `POST /photos/:slot` (multipart `photo`, тот же multer-паттерн,
       что у рецептов) — slot проверять по списку из 8 выше, иначе 400;
     - `DELETE /photos/:slot` — сброс на встроенную заглушку;
     - `GET/POST /faq`, `PUT/DELETE /faq/:id` — CRUD вопросов
       (сортировка по порядку добавления/`sort_order`, reorder — по
       желанию, не обязателен для первой версии).
  4. Админка — расширить существующую панель «Поддержка» → «Гид»:
     сетка из 8 подписанных превью (название слота человеческим языком,
     например «Обложка — цезарь», «Карточка блюда — карбонара» и т.п.)
     с кнопками «Загрузить»/«Сбросить»; ниже — список FAQ с
     добавлением/редактированием/удалением вопроса и ответа.
  5. Проверить: страница гида (и на сайте, и через
     `jigulibar.chefplan.ru/guide/`) показывает загруженное фото и
     вопросы сразу после сохранения в админке, без пересборки/деплоя
     веба — это чисто данные, а не правка файла.
- **Гид и FAQ — разделить на два раздела меню + ссылка FAQ в шапке
  гида — СДЕЛАНО (2026-09-25, ветка `zhiguli`, коммит `57b64f8`), НЕ
  ЗАДЕПЛОЕНО.** Пользователь попросил: в меню администратора платформы
  окно «Поддержка» → «Гид» (ссылка + фото + «Частые вопросы» одним
  окном) разбить на два самостоятельных раздела — «Гид» отдельно,
  «Вопросы» отдельно; в самой странице гида рядом с логотипом добавить
  ссылку «FAQ», чтобы сразу пролистать к блоку вопросов, не скролля
  вручную.
  Сделано: боковое меню платформы теперь Заведения → Поддержка → Гид →
  Вопросы (`web/src/components/Layout.tsx`, те же 4 пункта видны только
  при `is_platform_admin && !me?.tenant`, как раньше). `Гид`
  (`web/src/pages/Guide.tsx`) — ссылка и редактор фото, `Вопросы`
  (`web/src/pages/Faq.tsx`) — список вопрос/ответ с порядком; оба —
  обычные страницы с маршрутами `/guide` и `/faq` в `App.tsx`, а не
  модалки. `components/GuideModal.tsx` удалён, кнопка «Гид» и она сама
  убраны из `Support.tsx` — там остались только тикеты и уведомления.
  API не менялся (`/api/platform/guide/...`), только то, какой
  компонент его вызывает.
  На публичной странице гида (`web/public/guide/index.html`): рядом с
  логотипом в шапке — ссылка «FAQ» (`#faqLink`, якорь на `#faq`),
  скрыта пока вопросов нет (`display:none` по умолчанию), появляется
  одновременно с самим блоком в той же ветке скрипта, что рисует
  вопросы, и по клику проскролливает прямо к блоку (`scroll-behavior:
  smooth` уже стоит на `html`).
  Проверено: `tsc -b --noEmit` в `web/` проходит чисто; Playwright —
  ссылка скрыта при пустом FAQ, появляется при непустом, клик доводит
  ровно до блока.
  **Нужно от сессии с доступом к серверу:** `git pull` ветки `zhiguli`,
  пересобрать веб (`npm run build`), передеплоить; открыть боковое меню
  администратора платформы — должны появиться отдельные «Гид» и
  «Вопросы»; на `jigulibar.chefplan.ru/guide/` с непустым списком
  вопросов — проверить, что ссылка «FAQ» в шапке появляется и
  прокручивает к блоку.
  **ЗАДЕПЛОЕНО 2026-09-24 с одной поправкой (коммит `3e7c413`):** маршруты
  админки перенесены с `/guide` и `/faq` на **`/admin/guide`** и
  **`/admin/faq`**. Причина: `/guide` на сервере занят статикой публичного
  гида (`express.static('/guide')` отвечает 301 на `/guide/`) — переход из
  меню работал, но перезагрузка страницы или открытие по ссылке уводили
  администратора на публичный гид вместо админки. Новые SPA-маршруты на
  `/guide…` не заводить. Проверено в Playwright: меню платформы на
  десктопе и нижние вкладки на телефоне — Заведения/Поддержка/Гид/Вопросы,
  в «Поддержке» кнопки «Гид» больше нет, оба раздела переживают
  перезагрузку; на проде `/guide/` с вопросами — «FAQ» рядом с логотипом
  (десктоп и 390 px, без горизонтальной прокрутки), клик/тап доводит до
  блока; без вопросов или при ошибке API ссылки и блока нет (проверено
  подменой ответа — на проде уже 2 настоящих вопроса и 2 фото, их не трогали).
- **«Жигули» — смена домена на `calc.chefplan.ru` — СДЕЛАНО 2026-09-24
  (сервер), ждёт шагов пользователя.** Новый nginx-конфиг
  `/etc/nginx/sites-available/calc` → `127.0.0.1:3008`, сертификат
  Let's Encrypt (до 23.12.2026, автопродление), HTTP → HTTPS;
  `PUBLIC_BASE_URL=https://calc.chefplan.ru` в `server/.env` (копия
  прежнего — `.env.bak-20260924-calc`), pm2 `zhiguli` перезапущен.
  Проверено: `https://calc.chefplan.ru` (вход), `/guide/` (все картинки,
  2 своих фото и 2 вопроса из админки, ссылка FAQ), API. Первая попытка
  certbot упала — один из NS Timeweb (`ns3.timeweb.org`) и Google DNS
  ещё не видели запись; через ~5 минут прошло. **Старый
  `jigulibar.chefplan.ru` оставлен РАБОЧИМ, не редиректом**: вебхук
  @DEMOKUXNYAbot и кнопка мини-аппа пока на старом домене (Telegram не
  идёт по редиректу вебхука — бот бы замолчал). **Осталось пользователю:**
  (1) «Гид» → ссылка на `https://calc.chefplan.ru/guide/`; (2) перенастроить
  бота — «Заведения» → бот → «Отключить» → «Включить» (или заново токен).
  **Потом (отдельное решение):** старый домен → 301 на новый; вход на сайте
  на новом домене — заново (сессия хранится по домену).
- **Роль "технолог"** — команда на реализацию поставлена другой сессии
  (2026-09-24): добавить роль с доступом на чтение к актам разделки
  (без кнопок согласования), вход на сайт, скрыть нерелевантные пункты
  меню ("Смена", "План заготовок"). Личные Telegram-уведомления для
  этой роли сознательно НЕ включены — отдельное решение отложено.
  **Ответ от другой сессии ещё не получен в разговоре.**
- **Тарифы** — Excel готов, ждёт, когда пользователь распределит функции и
  назначит цены (см. п.21).
- **Контент-план** — концепция согласована (см. п.22), тексты постов не
  написаны, ждёт команды продолжать.
- **Старые debug-логи** (34 тыс. строк с личными данными) — пользователь
  ещё не решил, чистить или нет (см. п.20).
- **Два свободных профиля поваров в ОКО** — нужно выдать им новые коды
  приглашения через панель (операционное действие пользователя, не код).
- **«Жигули» — интеграция с iiko — СДЕЛАНО и задеплоено (2026-09-25,
  ветка `zhiguli`, коммит `f96a433`; подробности — README ветки
  `zhiguli`, «Шаг 7»).** Как сделано: модуль `iiko` в `MODULES`
  (выкл. по умолчанию, включается в «Модулях» заведения на панели
  платформы); подключение заводит владелец заведения (кнопка «iiko»
  рядом с «Импорт» на странице ТТК), таблица `tenant_iiko`, пароль —
  `secretBox`, сохраняется только после успешного входа в iiko; все
  `/api/iiko/*` — только владелец и только при включённом модуле.
  Импорт — как в спецификации ниже (актуальная версия карты, PREPARED →
  полуфабрикат, брутто = нетто = `amountIn`, потери 0, единицы из
  `measureUnits`, ПФ-связи автоматически, повтор без дублей по
  `iiko_product_id`), после работы — `logout` из iiko (не держим
  лицензию). TLS — обычная проверка, только `https` и только внешние
  адреса. **Решения, которые стоит подтвердить:** (1) если ручная карта
  с тем же названием уже есть — импорт берёт её под iiko, а не создаёт
  вторую (технология/фото/категория не трогаются); (2) блокировка правки
  (название, тип, выход, состав) действует, пока модуль включён — если
  модуль выключить, карты и связи остаются, но снова правятся руками
  (иначе застыли бы навсегда без синхронизации). Проверено: 49 API-сценариев
  на фейковом iiko, живой импорт с демо-стенда iiko KitchenDesk
  («Сливочно-творожный крем» → ПФ, брутто = нетто, 0 %), Playwright.
  Гид (раздел 04) — одно предложение про прямое подключение.
  **Баг «адрес iiko указывает во внутреннюю сеть» — ИСПРАВЛЕН
  (2026-09-25, `zhiguli` коммит `8cf662f`).** Проверка ошибочно
  блокировала любой публичный адрес, в т.ч. `542-903-605.iiko.it`
  (195.234.211.74). Причина: в списке запрещённых подсетей
  (`server/src/services/iikoApi.js`) было правило `::ffff:0:0/96`
  (IPv4-mapped IPv6), а Node `BlockList` сверяет с ним и обычные
  IPv4-адреса — совпадало всё. DNS резолвился верно. Правило убрано
  (mapped-адреса вроде `::ffff:10.0.0.1` BlockList и так сверяет с
  IPv4-правилами — они по-прежнему блокируются). Почему не поймал
  раньше: живой прогон шёл на тестовом экземпляре с `IIKO_ALLOW_LOCAL=1`,
  где проверка отключена. После фикса проверено на проде
  (calc.chefplan.ru, без `IIKO_ALLOW_LOCAL`), на временном заведении
  (удалено): `542-903-605.iiko.it` + `KDAPI26` — подключение 200,
  проверка 200, импорт 3 карты; localhost/10.x/172.16/192.168/
  169.254/::1/fd00::/`::ffff:10.0.0.1` — по-прежнему 400.
  **Исходная постановка — ниже, без изменений.**
  ~~Задача поставлена, НЕ СДЕЛАНО (2026-09-25).~~ В коде ветки `zhiguli` интеграции с iiko нет вообще —
  ни файла. Прежде текст гида и письмо клиенту описывали её как готовую
  фичу — это была ошибка, поправлено, реальной функции пока нет.
  **Откуда взяты факты ниже (важно для доверия к ним):** у меня (эта
  сессия) нет доступа к серверу KitchenDesk, только к git. В этом же
  репозитории на ветке `claude/oko-iiko-integration` лежит план/фрагменты
  кода iiko-интеграции KitchenDesk (реализовано было на сервере, не в
  git) — оттуда взяты авторизация и энд-пойнты. Данные о том, что реально
  приходит с iiko (брутто=нетто, %потерь=0 по ингредиенту) — не из кода,
  а с живых скриншотов актуальной версии KitchenDesk, которые прислал
  пользователь по запросу (карточка «Сливочно-творожный крем»,
  синхронизированная с iiko: у каждого ингредиента брутто точно равен
  нетто, %потерь везде 0) — **это проверено дважды**, теми же цифрами,
  что и в старом коде-плане, так что можно доверять.
  **Уточнение от пользователя (2026-09-25), важно — меняет пункт 1
  ниже: интеграция с iiko должна быть платным модулем, который
  включает/выключает администратор платформы на заведение, точно как
  уже сделано с «Пересчётом» (`server/src/modules.js`, реестр
  `MODULES`, `requireModule('<key>')` на сервере, `useModule('<key>')`
  во фронтенде, `LOCKED_MESSAGE` при выключенном модуле — см. как это
  устроено у `recalc`). Учётные данные при этом всё равно свои у
  каждого заведения (это не отменяет) — модуль лишь решает, доступна
  ли вообще заведению кнопка подключения к iiko, а не только у кого
  чьи логин/пароль.**
  **Что нужно реализовать (по образцу KitchenDesk, адаптировано под
  схему Жигулей — recipes/recipe_ingredients уже есть структурные
  brutto/netto/loss_percent/unit/linked_recipe_id, роль ПФ = recipes.kind
  = 'semi'):**
  1. Новый модуль `iiko` в реестре `MODULES` (`server/src/modules.js`):
     `{ title: 'Интеграция с iiko', description: 'Подключение
     заведения к iiko: импорт технологических карт и полуфабрикатов',
     defaultEnabled: false }`. Все роуты и экраны ниже — под
     `requireModule('iiko')`/`useModule('iiko')`, если модуль выключен —
     тот же `LOCKED_MESSAGE`, что и у «Пересчёта».
  2. Подключение к iiko — на заведение (по аналогии с уже
     существующими `tenant_bots`): своя таблица `tenant_iiko`
     (tenant_id, base_url, login, password_encrypted — тем же
     `secretBox`, что у токенов ботов, last_test_ok_at). CRUD только для
     владельца заведения (`requireRole('owner')`) **и** только если
     модуль `iiko` включён — в отличие от KitchenDesk, где это делает
     суперадмин платформы: в Жигулях у каждого заведения свои учётные
     данные iiko, платформенный админ их не заводит, только включает/
     выключает сам модуль. Тест соединения — тем же способом, что ниже.
  3. Авторизация в iiko: `GET {base_url}/resto/api/auth?login=...&pass=<SHA1(password)>`,
     токен из ответа передавать в следующих запросах как `key=<token>`.
     **Важно:** если креды берутся из `server/.env` для чего-то общего
     (маловероятно, обычно per-tenant) — значения с `#` (в пароле)
     обязательно в кавычках, `dotenv` иначе обрежет по `#` как
     комментарий, авторизация тихо сломается без понятной ошибки.
  4. Тянуть: `assemblyCharts` (техкарты), `products` (блюда/
     ингредиенты/полуфабрикаты — по ним же определять кто ПФ, смотреть
     на реальный признак типа продукта в ответе, не считать заранее
     какое там точное имя поля), `measureUnits` (единицы измерения).
  5. Выбор актуальной версии карты — самый поздний `dateFrom`, у
     которого `dateTo` ещё не наступил (не просто последняя по списку —
     на демо-стенде KitchenDesk наивный выбор давал не ту карту).
  6. Импорт в `recipes`/`recipe_ingredients`:
     - `recipes.name` = имя продукта, `kind` = 'semi' если продукт —
       полуфабрикат в iiko, иначе 'dish';
     - `recipe_ingredients.brutto` = `recipe_ingredients.netto` =
       `item.amountIn` (iiko отдаёт вес, не брутто/нетто раздельно —
       см. проверенный факт выше), `loss_percent` = 0, `unit` — по
       `measureUnits`;
     - `linked_recipe_id` — если ингредиент сам оказался среди
       импортированных ПФ этого же заведения, проставлять ссылку
       автоматически (так связка ПФ работает и без импорта — см. гид).
     - На каждый импортированный рецепт — пометка "управляется iiko"
       (аналог `iiko_managed_at` из плана KitchenDesk) с датой.
  7. Поведение при синхронизации — **по умолчанию как в KitchenDesk**:
     если карта пришла из iiko, её состав и выход нельзя редактировать
     вручную в Жигулях (только смотреть) — правки только через саму
     iiko, повторным импортом. Если пользователь захочет иначе (разрешить
     переопределять вручную) — отдельно скажет, пока делать как есть.
     Если позже модуль `iiko` выключат обратно администратору
     платформы — уже импортированные карты и связи ПФ остаются как
     есть (не удалять), просто подключение/повторный импорт становится
     недоступен, пока модуль снова не включат.
  8. TLS: боевые заведения — с нормальной проверкой сертификата
     (`rejectUnauthorized` не отключать) в отличие от демо-стенда
     KitchenDesk, где это было осознанно упрощено для теста.
  9. UI: в «ТТК» — кнопка/раздел «Подключение к iiko» у владельца
     (по аналогии с существующим импорт-редактором «два из трёх» —
     эта форма ручного дозаполнения `netto`/`loss_percent` после
     импорта уже есть и трогать её не нужно, ей просто будут
     пользоваться чаще после автоимпорта).
  10. Обновить гид (`web/public/guide/index.html`, раздел 04 «Быстрый
      старт») и превью в мок-карточке редактора, если реальное поведение
      после реализации будет расходиться с текущим текстом — сверить и
      поправить.
  Проверить перед деплоем: happy-path подключения, 403 не-владельцу,
  403/скрыто при выключенном модуле `iiko`, 404 на тест пустого/ещё не
  настроенного подключения, повторный импорт не плодит дубликаты
  (обновляет существующие по `iiko_product_id`), ПФ-связи проставляются
  автоматически, выключение модуля не удаляет уже импортированные
  данные.
- **«Жигули» — iiko: дерево категорий при импорте + автообновление по
  расписанию — СДЕЛАНО и задеплоено (2026-09-25, ветка `zhiguli`,
  коммит `d9edaaf`; подробности — README ветки `zhiguli`, «Шаг 8»).**
  Как сделано: (1) дерево папок iiko с галочками в окне «iiko» →
  «Папки iiko: Выбрать» — произвольная глубина, галочка родителя
  отмечает ветку, по умолчанию всё отмечено (`tenant_iiko.group_ids`
  = NULL → импорт всего); фильтр — прямое совпадение `parent`, как в
  PART-13; в сводке и тосте — «вне выбранных папок N»; уже
  импортированное не удаляется. **Отличие от PART-13, стоит знать:**
  добавлен пункт «Без папки» со своей галочкой — на демо-стенде iiko
  все 3 техкарты лежат вне папок (там единственная папка «Сервисный
  сбор» без карт), и без этого пункта любой сохранённый выбор отрезал
  бы всё. (2) Автообновление — каждую ночь около 04:00 по Ташкенту, тот
  же импорт с учётом папок, для каждого активного заведения с
  включённым модулем и подключением; пропущенный из-за перезапуска
  прогон догоняется в тот же день; ошибка одного заведения пишется в его
  `auto_error` и в лог pm2 (`[iiko auto] ...`) и не останавливает
  остальных; владелец видит «последнее автообновление / ошибка» под
  кнопкой импорта. Проверено: 39 сценариев на фейковом iiko (в т.ч. 5
  заведений: два рабочих не мешают друг другу, сломанное не роняет
  прогон, выключенный модуль/нет подключения — пропуск, раз в сутки),
  Playwright (десктоп+телефон), на проде — дерево и импорт на демо-стенде,
  первый автопрогон сам прошёл на заведении 4 (3 карты без изменений).
  **Исходная постановка — ниже, без изменений.**
  ~~Задача поставлена, НЕ СДЕЛАНО (2026-09-25).~~ Пользователь
  протестировал живьём: сейчас импорт тянет вообще всё из iiko без
  разбора — нужно как в KitchenDesk, где есть дерево папок с галочками,
  чтобы не затягивать лишнее (например, барные позиции вместе с
  кухонными). Второе — обновление карт сейчас только вручную по кнопке,
  нужно ещё и само по расписанию.
  **1. Дерево категорий — не выдумывать заново, в этом же репозитории
  уже есть проверенная схема** (ветка `claude/oko-iiko-integration`,
  файл `PART-13-scope-import-by-category.md`, статус — реализовано и
  задеплоено в KitchenDesk). Пересказ оттуда, адаптировать под схему
  Жигулей:
  - Эндпоинт `GET {base_url}/resto/api/v2/entities/products/group/list`
    — список групп/папок, у каждого товара есть поле `parent` со
    ссылкой на группу; группы могут быть вложены произвольно глубоко
    (не проектировать только под 2 уровня).
  - После успешного подключения — подтянуть дерево групп целиком,
    показать владельцу как дерево с чекбоксами (раскрывающееся, не
    плоский список).
  - По умолчанию — **всё дерево отмечено галочками** (не пустой список
    с нуля): владелец снимает галочки с ненужных подпапок, а не
    собирает список вручную с нуля.
  - Сохранить выбор — новое поле на `tenant_iiko` (аналог
    `iiko_kitchen_group_ids`, множество id групп, jsonb), аддитивно к
    уже существующим `base_url`/`login`/`password_encrypted`.
  - При импорте — товар импортируется, только если его `parent`
    входит в сохранённый набор (прямое совпадение, без обхода вверх
    по иерархии — выбор на уровне конкретных подпапок).
  - Товар без техкарты не рассматривается вообще (привязка идёт от
    `assemblyCharts`, не от полного каталога) — специальной логики не
    нужно, уже покрыто тем, что подтягиваются только карты с реальным
    `assemblyChart`.
  - После импорта — в тосте/ответе показать, сколько карт пропущено
    из-за невыбранной группы (чтобы владелец видел, что фильтр
    сработал, а не решил, что часть меню потерялась).
  - Предпросмотр (если/когда появится в Жигулях) фильтровать тем же
    набором, что и импорт — **кроме** самого первого подключения, пока
    ни одна группа ещё не выбрана (тогда без фильтра, чтобы было от
    чего оттолкнуться при первом выборе).
  - Уже импортированные раньше карты (до появления дерева) этим не
    чистятся задним числом — только будущие импорты фильтруются.
  **2. Автообновление по расписанию — готового решения нет нигде (ни в
  KitchenDesk, ни в Жигулях), проектировать с нуля:**
  - Раз в сутки (время — на усмотрение сессии, разумно ночью/рано
    утром, когда заведения не пользуются системой активно) для каждого
    заведения с включённым модулем `iiko` и настроенным, рабочим
    подключением — молча выполнять тот же импорт, что и по кнопке
    «Импорт», с учётом фильтра по дереву категорий из пункта 1.
  - Цикл по всем подходящим заведениям с `try/catch` на каждое — сбой
    одного не должен останавливать остальные (по образцу
    `syncAllCommands` в `bots.js`).
  - Хранить время последнего успешного автообновления (и, если было
    неудачным, — последнюю ошибку) на `tenant_iiko`, показывать
    владельцу рядом с кнопкой «Импорт» — то же по духу, что
    `last_test_ok_at`.
  - Не требуется отдельный тумблер вкл/выкл автообновления на первой
    версии — работает всегда, пока модуль и подключение активны; если
    понадобится ручное отключение — отдельная задача по запросу.
  - Проверить: два заведения с включённым iiko не мешают друг другу,
    заведение с выключенным модулем/без подключения пропускается,
    ошибка на одном заведении не роняет весь прогон и видна в логе.
- **«Жигули» — себестоимость блюда через каталог товаров — ТОЛЬКО ЗАДЕЛ
  НА БУДУЩЕЕ, НЕ НАЧИНАТЬ (2026-09-25).** Пользователь попросил
  зафиксировать проработанный план, чтобы спокойно начать позже —
  сейчас не заказ на реализацию. Повод: реальному заведению (шеф-повар,
  у них R-Keeper, не iiko) нужно видеть себестоимость блюда, но вписывать
  вручную нереально — слишком много товаров. Доступа к API R-Keeper нет
  и не предвидится (см. запись про R-Keeper выше) — обсуждали файловый
  путь (выгрузка из R-Keeper в Excel), но в разговоре родилась более
  правильная и общая архитектура, независимая от источника.
  **Идея (подтверждена пользователем как основная — «как в iiko»):**
  вместо того чтобы угадывать связь между текстом в `recipe_ingredients.name`
  и строкой прайс-листа, завести **отдельный каталог товаров на
  заведение** (новая таблица, условно `tenant_products`: id, tenant_id,
  name, purchase_price, purchase_unit — кг/л/шт/упаковка, и **отдельно**
  `unit_content` — вес/объём одной единицы прихода в базовых
  единицах (г/мл), обязательно для упаковки/штучного прихода, если
  расход в рецепте в граммах/литрах: без этого поля конвертация
  математически невозможна и делать её нельзя). `recipe_ingredients`
  получает необязательную ссылку `product_id` на этот каталог
  (аддитивно, не ломая текущие карты со свободным текстом).
  **Как заполняется каталог — три источника, один и тот же каталог:**
  1. Вручную — простой экран «Товары» у владельца/шефа.
  2. Файлом (R-Keeper и любой другой экспорт в Excel/CSV) — построчно
     обновляет/создаёт записи каталога.
  3. Из iiko (для заведений с этим модулем) — из уже импортируемых
     `products`, если/когда подтвердится, что оттуда реально можно
     получить закупочную цену товара напрямую (см. следующий пункт).
  **Как каталог используется:**
  - **Ручное создание карты** — поле ингредиента становится
    автодополнением по каталогу заведения (как в iiko), а не свободным
    текстом; выбрал товар — система сама знает цену и переводит
    единицы, human не занимается математикой.
  - **Импорт карты** (из iiko или файла) — имя ингредиента ищется в уже
    существующем каталоге **точным совпадением**; совпало — связь и
    цена проставляются автоматически; не совпало — ингредиент остаётся
    текстом без цены, карта помечается как «себестоимость неполная,
    не хватает цены на: …» (не скрывать цифру, не выдавать её за
    точную — обсуждали, пользователь не возражал против такого
    поведения по умолчанию).
  - **Никакого нечёткого/угадывающего сопоставления названий** —
    осознанно отвергнуто: для денег неверный тихий матч хуже, чем явное
    "не сопоставлено, сопоставьте один раз вручную через то же
    автодополнение" (сопоставление запоминается на будущее).
  - **Полуфабрикаты (ПФ)** — их себестоимость не ищется в каталоге
    товаров напрямую (в каталоге сырья её и не может быть), а считается
    рекурсивно из ингредиентов самого ПФ и подставляется туда, где этот
    ПФ используется как ингредиент.
  **Открытое (не решено, требует проверки или ответа пользователя,
  когда дойдём до реализации):**
  1. Есть ли у iiko прямой способ узнать **текущую закупочную цену
     товара** (не через отчёт по продажам `ProductCostBase.ProductCost`
     из OLAP, который требует реальной истории продаж, а именно цену
     товара как такового) — нужно проверить на живом заведении с
     реальными данными, прежде чем полагаться на это как на источник
     для каталога заведений с iiko.
  2. Кто видит себестоимость — владелец точно, а шеф-повар в системе
     Жигулей это тот же «владелец» или нужна отдельная роль/доступ —
     пользователь ещё не ответил, уточнить перед реализацией.
  3. Формат файла для ручной загрузки каталога (R-Keeper и подобные) —
     дождаться реального файла/примера от пользователя, не
     проектировать парсер вслепую.
- **KitchenDesk — редизайн главного экрана мобильного мини-аппа
  ("Главная") — макет утверждён пользователем, нужна реализация в
  реальном коде (2026-09-26). Заказ на реализацию, не задел на будущее.**
  Пользователь спроектировал новый вид экрана "Главная" (тот, что видит
  сотрудник в Telegram Mini App `/cabinet`, вкладка "Главная" нижнего
  таб-бара — см. `oko-presentation/screenshots/bot/01-mobile-dashboard.png`
  на branch `claude/oko-presentation-assets` для вида ДО редизайна) через
  отдельный визуальный инструмент (не эта кодовая база — макет нигде не
  закоммичен, поэтому вся раскладка и все значения ниже описаны текстом,
  это единственный источник истины). Просьба реализовать точь-в-точь в
  реальном компоненте главного экрана мини-аппа (по прежним заметкам в
  этом файле это, по всей видимости, тот же React-компонент, что рисует
  `/web` и `/cabinet` — сайт и мини-апп используют одни и те же
  компоненты, разница только в авторизации; вероятный путь по старому
  референсному снэпшоту фронтенда — `src/components/features/Dashboard.tsx`,
  но актуальный путь в реальном репозитории нужно проверить на месте,
  этот снэпшок не обязательно совпадает с текущим кодом).

  **Общая идея экрана:** фото-шапка сверху (как самостоятельный "баннер"
  с закруглёнными нижними углами — не на всю высоту экрана, среднего
  размера), под ней — уведомления, которые визуально "выезжают шторкой"
  прямо из-под этой шапки (не отдельная карточка в контенте), дальше —
  обычный скроллящийся контент (показатели, быстрые действия, система),
  внизу — нижний таб-бар. Три состояния экрана описаны отдельно ниже —
  это один и тот же экран в разные моменты (нет уведомлений / есть
  непрочитанные, свёрнуто / уведомления развёрнуты по тапу), а не три
  разных экрана.

  **Цветовые токены (тёмная тема, весь экран):**
  ```
  --bg:        #0a0f1a   (фон экрана)
  --surface:   #121a2b   (карточки — показатели, плитки быстрых действий)
  --surface-2: #182338   (иконки-квадраты внутри "Системы", фон шторки уведомлений)
  --line:      rgba(255,255,255,0.07)   (тонкие разделители/рамки карточек)
  --text-primary:   #f5f7fa
  --text-secondary: #aab4c4
  --text-tertiary:  #7c869c
  --blue:   #2f6fed   --blue-deep:   #1c4bb8
  --orange: #e08a3c   --orange-deep: #b96a24
  --green:  #22c55e   --green-deep:  #178a43
  --red:    #c24444   --red-deep:    #8f2f2f
  --link:   #4f8bff   (цвет ссылки "Все разделы →")
  ```
  Шрифты: заголовок-приветствие ("Алишер") — теплый акцентный шрифт с
  засечками (Google Fonts Fraunces, вес 600), весь остальной интерфейсный
  текст — Public Sans, цифры-показатели (ТТК/Цехов/Поваров) — моноширинный
  IBM Plex Mono вес 700, с `font-variant-numeric: tabular-nums`.

  **1. Фото-шапка ("баннер").** Высота ~210px, во всех трёх состояниях
  одинаковая (не сжимать/растягивать между состояниями — на предыдущей
  итерации макета шапка драматично уменьшалась под уведомления, это была
  ошибка, пользователь явно попросил зафиксировать один размер).
  Скруглены **только нижние** углы (`border-radius: 0 0 28px 28px`,
  верхние углы квадратные — шапка сидит вплотную к самому верху экрана).
  **Добавить тонкую окантовку** по периметру шапки (~1–1.5px,
  `rgba(255,255,255,0.14)` или близко) — без неё баннер визуально сливается
  с тёмным фоном экрана, это последнее замечание пользователя, ещё не
  реализовано ни в одной версии. Поверх фото — затемняющий градиент сверху
  вниз (прозрачный/лёгкий вверху, почти непрозрачный тёмный внизу,
  `rgba(6,10,20,0.1)` → `rgba(6,10,20,0.3)` на середине → `rgba(6,10,20,0.92)`
  внизу) для читаемости текста.
  **Фото в шапке — фото самого заведения, не кухни** (это тоже последнее
  замечание пользователя — раньше стояло фото кухни, `bg-kitchen.jpg` из
  `web/public/guide/img/` в ветке `zhiguli`, для другого проекта "Жигули";
  пользователь явно сказал, что в шапке должно быть фото заведения, а не
  кухни). **Файла фото заведения у этой сессии нет, но пользователь
  подсказал, где искать: оно уже есть в "Пособии для официантов"**
  (`oko-waiter-guide` — см. `oko-waiter-guide/README.md` в этом же
  репозитории, ветка `claude/oko-iiko-integration`, для описания модуля;
  сами фото не в git, лежат на сервере/в БД этого модуля, раздаются
  через `GET /photos/:filename` без пароля). Нужно на сервере найти в
  этом модуле подходящее фото самого заведения (зал/фасад — не фото
  конкретного блюда) для того же заведения/тенанта, которому принадлежит
  редизайнимая "Главная", и переиспользовать его как фон шапки. Если для
  этого тенанта в пособии официанта фото зала/фасада не найдётся (там по
  умолчанию только фото блюд) — уточнить у пользователя, а не гадать и
  не оставлять кухню молча.
  Поверх фото, **одной горизонтальной группой, вертикально отцентрованной
  по середине высоты шапки** (не аватар вверху отдельно + текст внизу
  отдельно — так было в предыдущей версии макета, пользователь явно
  попросил объединить в одну группу и центровать): круглый аватар
  сотрудника (60×60px, зелёная точка "онлайн" в правом нижнем углу
  аватара, обводка 2px `rgba(255,255,255,0.25)`) слева, справа от него
  вертикальный текстовый блок — имя (24px, Fraunces 600, белый) и строкой
  ниже "Должность · Цех" одной строкой через " · " (13px,
  `rgba(255,255,255,0.78)`). Группа с отступом `left/right: 20px` от краёв
  шапки.

  **2. Состояние "нет уведомлений".** Сразу после шапки — обычный
  скроллящийся контент с отступами по бокам 16px: (а) ряд из 3 карточек-
  показателей (сетка 3 колонки, gap 10px, каждая — фон `--surface`, рамка
  `--line`, радиус 16px, внутри — цветной квадрат-иконка 34px сверху,
  подпись мелким текстом, значение крупным моно-шрифтом, единица измерения
  под значением: "ТТК" 351/рецепт, синяя иконка книги; "Цехов" 6/цехов,
  оранжевая иконка поварского колпака; "Поваров" 11/в команде, синяя
  иконка двух силуэтов); (б) заголовок "Быстрые действия" + ссылка "Все
  разделы →" справа (цвет `--link`); (в) сетка 2×2 плиток (gap 10px, фон
  `--surface`, радиус 16px, внутри — цветной квадрат-иконка 36px, затем
  название жирным 14px и подпись мельче 11px серым): "Поиск ТТК" / "Технологические
  карты" (синяя иконка лупы), "Чек-лист" / "Задачи смены" (зелёная иконка
  галочки в квадрате), "План заготовок" / "На завтра" (оранжевая иконка
  трёх слоёв), "Акты разделки" / "Создать акт" (красная иконка документа);
  (г) заголовок "Система" + сетка 4 колонок мини-иконок без фона-карточки
  (просто квадрат-иконка 40px на фоне `--surface-2` + подпись под ней):
  "Уведомления" (колокольчик), "План смены" (календарь), "Банкеты"
  (иконка-салют/звёздочка, оранжевый цвет иконки), "Документы" (документ,
  синий цвет иконки). Дальше — нижний таб-бар (см. пункт 5).

  **3. Состояние "есть уведомления" (свёрнуто).** То же самое, но сразу
  под шапкой — на всю ширину экрана (без боковых отступов), полосой,
  визуально прикреплённой к нижнему краю шапки без зазора, "шторка": фон
  **полупрозрачный** (не сплошной `--surface-2` как было — пользователь
  явно попросил уменьшить непрозрачность, чтобы полоса не бросалась в
  глаза, но оставалась заметной; ориентир — `rgba(24,35,56,0.72)` вместо
  сплошного `#182338`, можно уточнить на глаз при реализации), скруглены
  только нижние углы (`0 0 18px 18px`), лёгкая внутренняя тень сверху
  (`inset 0 10px 14px -12px rgba(0,0,0,0.5)`) — чтобы читалось как слой,
  выезжающий из-под шапки, а не отдельная плашка с своей рамкой в контенте
  (это тоже прямая правка пользователя — раньше уведомление было кнопкой
  с рамкой в контентной области, это неверно). Содержимое полосы —
  кликабельная строка (весь ряд — один тап-таргет): иконка колокольчика в
  квадрате 36px с маленькой красной точкой в углу, текст "Новые
  уведомления" (14px, жирный), справа — красный кружок-бейдж "3", затем
  иконка `>`. Тап по этой строке разворачивает третье состояние.

  **4. Состояние "уведомление развёрнуто".** Та же шторка, но выше —
  внутри неё сверху та же строка-заголовок, но текст "Уведомления" (без
  слова "Новые") и иконка справа теперь `^` (разворот вверх = "свернуть").
  Ниже — список из 3 пунктов, каждый с верхней тонкой линией-разделителем
  между пунктами (`rgba(255,255,255,0.05)`), слева цветной квадрат-иконка
  32px, по центру название жирным 13px + подпись серым 11px, справа время
  10px и маленькая синяя точка (непрочитано) друг под другом:
  1. Зелёная иконка коробки — "Приход товара" / "Поступление от ООО
     «ПродуктСервис»" / 10:24
  2. Оранжевая иконка треугольника-предупреждения — "Списание" /
     "Списано: Куриное филе — 2.5 кг" / 09:17
  3. Синяя иконка чат-пузыря — "Комментарий" / "Новый комментарий в акте
     разделки" / 08:45
  После шторки — точно тот же контент, что в состоянии "нет уведомлений"
  (показатели, быстрые действия, система) — они НЕ прячутся и НЕ меняют
  размер, просто уезжают вниз по экрану, потому что шторка выше. Экран в
  этом состоянии длиннее — это нормально и ожидаемо (в проекте макета
  это было отдельно проверено и одобрено пользователем), не пытаться
  втиснуть всё в одну высоту экрана.

  **5. Нижний таб-бар (одинаковый во всех трёх состояниях).** 6 вкладок:
  Главная (активна — синий закруглённый квадрат-подложка под иконкой,
  синяя подпись), ТТК, Акты, Склад, Банкет, Профиль (неактивные — серый
  цвет иконки и подписи, без подложки). Фон бара — `--surface`, верхняя
  линия-разделитель `--line`.

  **Проверить перед отчётом о готовности:** согласовать имя/должность/цех
  в шапке с реальными данными текущего залогиненного сотрудника (в макете
  для примера использовались "Алишер" / "Су-шеф" / "Горячий цех" —
  подставлять реальные значения из профиля, не хардкодить); числа в
  показателях и плитках (351/6/11, "11 карт" на плитке поиска и т.п.) —
  тоже подставлять реальные данные заведения, в макете это условные
  примерные цифры.

  **СДЕЛАНО и задеплоено 2026-09-26** (kitchendesk.chefplan.ru, `/` и
  `/cabinet` — один и тот же бандл). Фронтенд по-прежнему не в git
  (`/home/oko-kitchen/oko-kitchen/oko-frontend` — один коммит "Add files via
  upload", весь `src/` untracked), поэтому "коммита" нет — live-edit + `npm
  run build` + `rsync --delete` в `/home/kitchendesk/frontend/` с шестью
  `--exclude` (waiter-guide, shelf-life, print-admin, oko-order, inventory,
  client-oko-logo.jpg; dry-run перед деплоем показал ровно их). Бэкапы
  оригиналов — `*.bak-redesign-20260926-115452` рядом с каждым файлом.
  - **Актуальный путь** уточнён на месте: мобильная "Главная" — это
    `src/components/features/Dashboard.tsx` (рендерится из `src/app/page.tsx`,
    это и есть `/cabinet`). `/web` рисует ДРУГОЙ компонент —
    `src/components/dashboard/WebDashboard.tsx`, его не трогали (прежняя
    заметка "тот же компонент, что /web" для Главной неверна).
  - `Dashboard.tsx` переписан по макету: токены/размеры/шрифты как выше
    (CSS строкой `HOME_CSS`), шапка 210px одинаковая во всех состояниях,
    `0 0 28px 28px`, окантовка 1.5px `rgba(255,255,255,0.14)` (сверху нет —
    шапка у края экрана), градиент 0.1→0.3→0.92, аватар 60px + имя/
    "Должность · Цех" одной группой по центру. Шторка — `rgba(24,35,56,0.72)`
    + blur, `0 0 18px 18px`, inset-тень, подныривает под шапку
    (`margin-top:-28px`). Три состояния — одно и то же: шторки нет, если
    непрочитанных 0; свёрнута "Новые уведомления" + бейдж + `>`; по тапу
    "Уведомления" + `^` и 3 последних уведомления.
  - **Данные реальные, не из макета:** имя/роль/цех — из профиля
    (`user.name/role/section`); 351/6/11 — `GET /api/profile/stats`;
    уведомления — та же лента `GET /api/notifications`, что колокольчик на
    `/web` (`useNotificationsFeed`, поллинг 30с, тап по пункту = прочитано).
    Примеры из макета ("Приход товара"/"Списание"/"Комментарий") в коде не
    захардкожены — реальные типы ленты сейчас `plan`, `shift_report`,
    `duty_summary`, `ticket`; иконки/цвета по типу в `NOTIF_ICON`.
  - **Фото шапки** — `restaurants.cover_url` текущего заведения (то же поле,
    что баннер на `/web`, меняется в "Управление → Заведение"). Для ОКО
    (id 1) по решению пользователя обложка заменена на фото зала из
    пособия официантов: `waiter-guide/hero/hero-2.jpg` (горизонтальный общий
    вид зала) скопирован в `/home/oko-kitchen/photos/venue_cover_1_1790423668583.jpg`
    и прописан в `cover_url` (прежняя обложка hero-1 — файл
    `venue_cover_1_1790259905741.jpg` не удалён). У заведений без обложки
    шапка — тёмный градиент без фото; кухню нигде не подставляем.
  - **Нижний бар** (`src/components/layout/BottomNav.tsx`, общий для всех
    экранов): по решению пользователя взят только стиль макета — сплошной
    `#121a2b`, линия сверху, синяя подложка `#2f6fed` под активной иконкой,
    синяя подпись, серые неактивные. Набор пунктов и роли прежние (у
    руководителя 7: Главная, ТТК, Акты, Склад, Банкет, Профиль, Управление;
    у повара 7: …Смена, План…, без Банкета). Стеклянный вариант со
    скользящим индикатором снят (он в бэкапе).
  - Отступления от буквы макета, осознанные: Fraunces и Public Sans не
    содержат кириллицы — имя добирается шрифтом Lora 600 (подключён в
    `src/app/layout.tsx` вместе с остальными), остальной русский текст —
    системный sans; IBM Plex Mono кириллицу имеет. Руководителям Смена/План
    закрыты (`COOK_ONLY_TABS`), поэтому у них плитки "Чек-лист"/"План
    заготовок" ведут в Управление (Шаблоны чек-листа / Дата плана), у
    повара — в сами разделы. "Все разделы →" — Управление (руководитель) /
    Профиль (повар). Система: "Уведомления" — разворачивает шторку,
    "План смены" — Управление→График / Профиль (там "мой график"),
    "Банкеты" — только руководителям (как в баре, PART-8), "Документы" —
    Акты (отдельного раздела документов в приложении нет). Блок "Статус
    цехов" (monitor) с Главной убран — в макете его нет, он же есть в
    Управление → Мониторинг. Плашки онбординга/демо-режима и кнопка выхода
    (вне Telegram) сохранены.
  - Проверено: `tsc --noEmit`, `next build`, после деплоя новый чанк с
    `kd-drawer-head`/`kd-nav-item` отдаётся с прода, `/` и `/cabinet/` 200,
    фото waiter-guide не задеты. Визуально — скриншоты playwright 390×844
    на собранном `out/` с подставленными ответами API (руководитель с 3
    уведомлениями свёрнуто/развёрнуто, повар без уведомлений). Живьём под
    реальным аккаунтом в Telegram не смотрели — первый реальный просмотр
    за пользователем.
  - **Живой отзыв пользователя (2026-09-26, посмотрел в боте живьём):**
    три уведомления в `/cabinet` работают как задумано (свёрнуто/
    развёрнуто) — подтверждено на реальном аккаунте, не только на
    скриншотах. Одна явная правка: **фото сотрудника (аватар в шапке,
    сейчас 60px) сделать немного крупнее** — по ощущению пользователя
    мелковато рядом с именем/должностью. Насколько увеличить — на
    усмотрение реализующей сессии (например, 68–72px), лишь бы не
    отъедало место у текста рядом и не выходило за высоту шапки (210px).
    Пользователь также спросил про **APK-обёртку** (`oko-mobile-app/`) —
    сказал, что этого редизайна там нет, и попросил "продублировать".
    **Прежде чем это делать — важное несоответствие, требует уточнения у
    пользователя, не начинать вслепую:** по более ранней записи в этом же
    файле (п.39, "первая тестовая сборка мобильного приложения") APK
    сейчас открывает `TARGET_URL = https://kitchendesk.chefplan.ru/web`
    (админский `WebDashboard.tsx`, ОТДЕЛЬНЫЙ компонент от только что
    отредактированного `Dashboard.tsx` из `/cabinet`) — и то только для
    роли "Главный администратор"; вход в `/cabinet` через голый WebView
    в принципе не работает (Mini App проверяет крипто-данные настоящего
    Telegram, обёртка их дать не может). Значит "продублировать в APK"
    технически означает один из двух РАЗНЫХ заказов: (а) применить этот
    же визуальный редизайн к `WebDashboard.tsx` (админская версия на
    `/web`, её APK реально показывает) — тогда это не дубликат, а новая
    задача на другой компонент; или (б) научить APK открывать `/cabinet`
    для сотрудников — это требует отдельно решать проблему крипто-
    авторизации Mini App вне Telegram, ранее сознательно отложено (см.
    п.39), крупная задача сама по себе. Пользователь сказал "я сейчас
    ещё раз проверю" — дождаться уточнения, что именно он имеет в виду,
    прежде чем брать в работу.
  - **Уточнение от пользователя (тот же день, после живой проверки) —
    два новых пункта, оба реальные заказы, не вопросы:**
    1. **Шторка уведомлений не должна двигать остальной экран.** Сейчас
       разворачивание шторки (тап → список из 3 уведомлений) раздвигает
       документ — всё, что ниже (в первую очередь ряд показателей
       ТТК/Цех/Поваров), уезжает вниз по экрану. Пользователь явно
       попросил обратное: шапка (аватар/имя/должность/цех) И ряд
       показателей (ТТК/Цех/Поваров) должны остаться на месте, НЕ
       сдвигаться, когда шторка разворачивается или сворачивается —
       двигаться/меняться в размере должна только сама шторка. Технически
       это значит перевести шторку из обычного потока документа в
       перекрывающий слой (`position: absolute`/`fixed` с нужным
       `z-index`, разворачивается поверх контента снизу, а не раздвигает
       его) — реализация на усмотрение сессии с доступом к коду, важен
       именно результат: положение шапки и ряда показателей на экране не
       должно зависеть от состояния шторки.
    2. **Заодно фон самой шторки — сделать "прозрачным блюром"**, то есть
       не просто полупрозрачный цвет (`rgba(24,35,56,0.72)`, как сейчас),
       а стеклянный эффект с `backdrop-filter: blur(...)` поверх того, что
       под ней (проверить, что WebView в Telegram Mini App вообще
       поддерживает `backdrop-filter` — если нет, ближайший визуальный
       эквивалент).
  - **Про APK — ОТМЕНА предыдущей записи выше (пункт про решение
    авторизации сотрудника вне Telegram), пользователь уточнил и всё
    сильно проще, чем сначала показалось:**
    - **Логин в APK работает и для роли "шеф"/"су-шеф", не только для
      владельца** — пользователь лично проверяет APK, заходя именно
      под аккаунтом шефа через `/web-login`. Значит более раннее
      предположение в этом файле ("только Главный администратор может
      войти через /web-login", п.39) либо устарело, либо изначально
      было верно только для одного конкретного теста, а не является
      реальным ограничением системы — на практике `/web` пускает и
      других сотрудников с доступом к веб-панели, не только владельца.
      Решать проблему крипто-авторизации Mini App вне Telegram **не
      нужно** — она тут ни при чём, APK и так открывает `/web`, вход
      уже работает.
    - **Это значит, что и два пункта прямо выше (шторка не должна
      двигать шапку/показатели + блюр шторки) нужно делать сразу в
      `WebDashboard.tsx`, а не как отдельный патч в уже готовом
      `Dashboard.tsx`** — раз дальнейшая работа теперь только над APK.
      Если ту же правку легко и бесплатно занести и в уже готовый
      `Dashboard.tsx` заодно — не помешает, но это не обязательно и не
      то, чего пользователь сейчас просит.
    - **Реальная задача — просто переделать существующий баннер/дашборд
      в `WebDashboard.tsx`** (тот, что APK показывает на `/web`) под
      тот же визуальный стиль, что уже сделан в `Dashboard.tsx` для
      `/cabinet`: то же самое, что описано выше по всему этому пункту
      (токены, шапка 210px/скругление/окантовка, центрированная группа
      аватар+имя+должность+цех, стата ТТК/Цех/Поваров, шторка-оверлей с
      блюром, и т.д.) — один в один перенести на баннер `WebDashboard.tsx`.
      Там уже есть какой-то баннер/приветствие (см. исходный список
      экранов `/web`: "Главная — дашборд: приветствие, счётчики,
      быстрые действия" в `oko-iiko-integration/KITCHENDESK-SCREENS-GUIDE.md`)
      — не строить с нуля, а именно переделать то, что уже есть, под
      новый вид.
    - **Дальше работать только над APK/`/web`, `/cabinet` (мини-апп,
      `Dashboard.tsx`) больше не трогать для этой темы.** Пользователь
      прямо сказал: когда родное приложение (APK) заменит мини-апп
      полностью, мини-апп собирается убрать вообще — Telegram останется
      только каналом уведомлений, не основным интерфейсом. Значит с
      этого момента любые дальнейшие правки дизайна "Главной" (в т.ч.
      два пункта чуть выше — неподвижная шапка/показатели при шторке,
      блюр шторки) нужно вносить в `WebDashboard.tsx`, а не в уже
      готовый `Dashboard.tsx` — тот трогать было последний раз для этой
      задачи.
  - **Настоящая авторизация сотрудников в APK — ТОЛЬКО ЗАДЕЛ НА
    БУДУЩЕЕ, НЕ НАЧИНАТЬ (2026-09-26).** Пользователь явно сказал: это
    отдельный шаг для потом, не сейчас. Сейчас вход в APK через
    `/web-login` под конкретным сотрудником (сейчас проверяется под
    шефом) — этого достаточно, пока APK не готова полностью
    (функционал + дизайн). Когда APK будет полностью готова и
    пользователь начнёт переводить действующих сотрудников с
    Telegram-бота на неё (Telegram останется только каналом
    уведомлений) — тогда, и только тогда, нужна настоящая авторизация
    для реальных действующих сотрудников (не тестовый логин одного
    аккаунта) — то же самое, что раньше в п.39 называлось "одноразовый
    код + PIN". Не проектировать и не реализовывать сейчас — просто
    держать в уме как следующий большой шаг после того, как этот файл
    зафиксирует APK как готовую по функционалу и дизайну.
  - **СДЕЛАНО и задеплоено 2026-09-26 — редизайн Главной в
    `WebDashboard.tsx` (`/web`, её показывает APK), все три пункта сразу:
    перенос дизайна + шторка-оверлей + стеклянный блюр.** Как и раньше,
    фронтенд не в git — live-edit в `/home/oko-kitchen/oko-kitchen/oko-frontend`
    + `npm run build` + `rsync --delete` с шестью `--exclude` (dry-run перед
    деплоем чистый). Бэкапы — `*.bak-redesign-20260926-122114`.
    `Dashboard.tsx` (`/cabinet`) в этот раз НЕ трогали.
    - Изменённые файлы: `src/components/dashboard/WebDashboard.tsx`
      (переписан), `src/app/web/page.tsx` (две строки, см. ниже).
    - **Перенос:** те же токены, шрифты, шапка 210px (`0 0 28px 28px`,
      окантовка 1.5px, градиент 0.1→0.3→0.92, фото заведения из
      `restaurants.cover_url` — у ОКО это hero-2 из пособия), центрированная
      группа аватар + имя + "Должность · Цех", ряд ТТК/Цехов/Поваров, плитки
      2×2, "Система" 4 колонки, нижний бар (он общий, `BottomNav.tsx`, стиль
      уже был переделан в a6d8541). **Аватар — 70px** (было 60, по живому
      отзыву). Данные реальные — те же API, что в мини-аппе.
    - **Шторка — перекрывающий слой:** `position: absolute` внутри блока
      шапки, `z-index` ниже шапки (подныривает под её скругление) и выше
      контента. В потоке документа под ней стоит только пустой слот
      фиксированной высоты свёрнутой полосы (60px), поэтому положение шапки
      и ряда показателей от состояния шторки не зависит вообще;
      развёрнутый список (3 последних уведомления) выезжает поверх
      "Быстрых действий" (плавно, `max-height`). Тап мимо шторки —
      сворачивает.
    - **Блюр:** `backdrop-filter: blur(18px) saturate(140%)` (+ `-webkit-`)
      на `rgba(24,35,56,0.5)`; если браузер `backdrop-filter` не умеет
      (`@supports not`) — почти сплошной `rgba(24,35,56,0.94)`. Android
      WebView (APK) — Chromium, он `backdrop-filter` поддерживает; в
      тестовом Chromium стиль реально применился (computed
      `blur(18px) saturate(1.4)`).
    - **Отступления:**
      1. Из-за п.2 (ничто не двигается) у "нет уведомлений" больше нет
         отдельного состояния без полосы: полоса видна всегда, при нуле
         непрочитанных — приглушённая "Нет новых уведомлений" без бейджа
         (тап всё так же раскрывает последние 3). Иначе при появлении/
         прочтении уведомлений показатели прыгали бы на 60px.
      2. На `/web` у Главной своя шапка с фото, поэтому общий
         `VenueBanner` на вкладке Главная больше не показывается (иначе
         было бы два баннера подряд) — на остальных вкладках он как был.
      3. Рабочие блоки прежней веб-Главной (Статус цехов, График, История
         планов) оставлены ниже "Системы", без изменений; старые
         `WelcomeCard`/`QuickActions` больше не подключены (файлы лежат).
      4. Кнопка выхода в углу шапки — только на мобильной ширине (в APK
         `Topbar` с меню профиля скрыт, выйти иначе негде); на десктопе
         (≥1024px) её нет, там шапка скруглена со всех сторон и с
         отступами, плитки 4 в ряд.
      5. Плитки/Система — как в мини-аппе: руководителю Чек-лист/План
         ведут в Управление (Шаблоны чек-листа / Дата плана), "План
         смены" — Управление→График / Профиль, "Документы" — Акты,
         "Банкеты" — `/banquet/` (только руководителям).
      6. **Попутный фикс:** пункт "Банкет" нижнего бара на `/web` (т.е. в
         APK) открывал пустой экран — в `app/web/page.tsx` не было
         рендера вкладки `banquet`. Добавлен тот же `<Banquet />`, что в
         мини-аппе (для руководителей).
    - **Как проверено:** `tsc --noEmit`, `next build`; playwright
      (Chromium) на собранном `out/` с подставленными ответами API, 390px
      и 1280px, руководитель с 3 непрочитанными и повар без уведомлений:
      замер `getBoundingClientRect().top` шапки / ряда показателей / плиток
      до и после раскрытия шторки — совпадает до пикселя (390px: 0 / 284 /
      475; 1280px: 91 / 375 / 566). После деплоя: `/`, `/cabinet/`,
      `/web/`, `/banquet/`, `/waiter-guide/` — 200, новый чанк `/web` с
      `kd-strip-slot` и blur отдаётся с прода. **Живьём в APK под
      аккаунтом шефа не смотрели** — войти под реальным сотрудником
      сессия не может, первый реальный просмотр за пользователем.
  - **Живой отзыв пользователя (2026-09-26, после проверки) — новая
    партия правок поверх уже сделанного в `caf1da9`:**
    1. **Отменить "пустую полосу" уведомлений.** Решение из `caf1da9`
       (показывать полосу всегда, с текстом "Нет новых уведомлений",
       когда непрочитанных нет — специально, чтобы шапка/показатели не
       прыгали) пользователю не понравилось визуально — портит вид.
       Вернуть как было: **шторки нет вообще**, когда непрочитанных
       уведомлений 0 — ни строки, ни намёка. Правило "шапка и показатели
       не сдвигаются" остаётся в силе **только для разворачивания/
       сворачивания уже видимой шторки** (когда есть хотя бы одно
       уведомление) — а вот сам переход "не было уведомлений → появилось
       одно" может отодвинуть контент вниз, это ожидаемо и не проблема.
    2. **Тот же баннер (фото/окантовка/скругление/размер), что на
       Главной, — на вкладках ТТК, Акты, Склад, Банкет** (НЕ Профиль —
       туда пока не трогаем, отдельно доработаем позже; НЕ Управление).
       Без приветствия/аватара/показателей — вместо них просто название
       раздела как заголовок, расположенный чуть ниже, чем стоит группа
       имя/аватар на Главной. Для ТТК заголовок — **"Технологические
       карты"** (не "Поиск ТТК" — это было название плитки на Главной,
       на самой вкладке ТТК так называть не надо). Для Акты/Склад/Банкет
       пользователь конкретный текст не давал — предложение (можно
       поправить на месте, не критично): "Акты разделки", "Склад",
       "Банкеты".
    3. **Кнопка выхода наверху на Главной — убрать совсем.** Не переносить
       никуда прямо сейчас — в профиль её добавим отдельной задачей
       позже, когда дойдём до раздела "Профиль". Сейчас просто убрать.
    4. **Склад — "Закуп" и "Списание" как вкладки-вставки под баннером,
       с эффектом открывающейся папки.** На вкладке Склад — под баннером
       (в том же месте, где на Главной сидит шторка уведомлений) —
       разместить два переключателя-вкладки: "Закуп" и "Списание".
       Визуально при нажатии на любую из них должно выглядеть как
       открытие/выдвижение вкладыша канцелярской папки (folder tab) —
       не просто смена контента, а с ощущением физического открытия
       конкретной "закладки". Это специфично для Склада — там внутри
       ещё есть несколько кнопок перехода на другие экраны, эффект нужен
       именно тут. Точная анимация/техника — на усмотрение реализующей
       сессии, ключевое — ощущение "вкладыша в папке", не просто таб-бар.
    5. **Всю эту партию (пункты 1–4 выше) сделать И в APK
       (`WebDashboard.tsx`), И в мини-аппе (`Dashboard.tsx`, `/cabinet`)
       — не только в APK, вопреки более раннему решению "дальше только
       APK".** Причина явно названа пользователем: ему нужно проверять
       в мини-аппе вкладки/поведение, которых нет у роли су-шеф (то есть
       тестировать разные роли через мини-апп с реальными Telegram-
       аккаунтами сотрудников — через APK так не проверить, там пока
       единственный тестовый вход). Дальнейшие решения "только APK" по
       умолчанию больше не считать действующими без явного
       подтверждения — уточнять при следующей похожей правке, если
       неясно.
    (Отдельная реплика пользователя "группу все скидывай, только где
    утварь то туда" в этом же разговоре — **не относится к задаче**,
    пользователь подтвердил, что это случайно записавшийся фрагмент
    постороннего голосового сообщения, не инструкция. Игнорировать.)
  - **СДЕЛАНО и задеплоено 2026-09-26 — партия правок из `4faefa2`, в
    обоих местах (APK `/web` и мини-апп `/cabinet`).** Фронтенд всё так же
    не в git — live-edit в `/home/oko-kitchen/oko-kitchen/oko-frontend`,
    `npm run build`, `rsync --delete` с шестью `--exclude` (dry-run перед
    выкладкой чистый). Бэкапы изменённых файлов —
    `*.bak-redesign2-20260926-125027`.
    - **Чтобы "оба места" больше не расходились, Главная стала одним
      компонентом:** новый `src/components/dashboard/HomeScreen.tsx`;
      `features/Dashboard.tsx` (мини-апп) и `dashboard/WebDashboard.tsx`
      (`/web`) теперь просто рендерят его (у `/web` снизу по-прежнему
      Статус цехов / График / История планов). Правки Главной дальше — в
      `HomeScreen.tsx`, сразу попадают и в APK, и в мини-апп.
      `OnboardingBanner` вынесен в `dashboard/OnboardingBanner.tsx`
      (реэкспорт из `Dashboard.tsx` оставлен).
    - **Фото-шапка — общий компонент** `src/components/layout/SectionHero.tsx`
      (210px, фото `restaurants.cover_url`, окантовка, скругление, градиент;
      на ≥1024px скруглена со всех сторон). Главная кладёт в неё аватар/имя,
      разделы — заголовок.
    1. **Пустой шторки больше нет:** при 0 непрочитанных шторки и её слота
       нет вообще, показатели сразу под шапкой. Когда шторка есть, это
       перекрывающий слой над слотом фиксированной высоты, поэтому
       разворот/сворот ничего не двигает. Шторка остаётся на экране, пока
       развёрнута, даже если прочитали последнее (чтобы список не исчез
       из-под пальца); после сворачивания при 0 уходит, контент
       поднимается — это допустимый переход "0 ↔ есть". Мини-апп тоже
       получил шторку-оверлей + blur (раньше там был поток документа).
    2. **Баннер на разделах** (Профиль и Управление не тронуты): ТТК —
       "Технологические карты" (`features/Search.tsx`), Акты — "Акты
       разделки" (`features/Acts.tsx` в мини-аппе, `features/WebActs.tsx`
       на `/web`), Склад — "Склад" (`features/Warehouse.tsx`, общий),
       Банкет — "Банкеты" + плашка "Бета" (`features/Banquet.tsx`, общий;
       та же шапка и на отдельной странице `/banquet`). Заголовок — Fraunces
       26px, центр на 62% высоты (группа аватар/имя на Главной — на 50%).
       Прежние `<h1>` этих экранов убраны (заголовок теперь в шапке);
       подзаголовки ("PDF отправляется администратору", "Всего актов")
       оставлены. На `/web` старый `VenueBanner` на этих вкладках больше не
       показывается (иначе два баннера), остался на Профиле/Настройках.
    3. **Кнопка выхода на Главной убрана совсем** (в обоих местах). На
       `/web`-десктопе выход по-прежнему в меню профиля в Topbar; на
       мобильном `/web`/APK выхода сейчас нет — ждёт задачи по Профилю.
    4. **Склад — вкладыши папки "Закуп"/"Списание"** под шапкой (на месте
       шторки): трапециевидные вкладыши (`clip-path`), выходят из-под
       скругления шапки; активный выдвинут вверх, светлее и сливается с
       "корешком" папки под ним, неактивный опущен, наклонён
       (`rotateX`) и темнее; переключение — упругая анимация выдвижения
       вкладыша (~420ms) + содержимое "вытягивается" из папки как лист
       (сдвиг сверху + раскрытие по высоте, ~380ms). Старые кнопки-табы
       внутри Склада убраны. Выбор вкладки по-прежнему в сторе
       (`warehouseTab`), deep-link `?tab=writeoff` работает как раньше.
    - **Отступления/заметки:** на ТТК строка с кнопкой фильтра осталась
      под шапкой (справа), на широком экране шапка ТТК — в левой колонке
      списка, у Банкета — в колонке списка 300px (так устроены эти
      экраны; растягивать шапку на обе колонки значило бы перестраивать их
      разметку). **Замечено, не трогал (было и до этого):** на `/web` у
      повара пункт "Склад" нижнего бара открывает пустой экран — в
      `app/web/page.tsx` Склад рендерится только руководителям, а бар
      показывает его всем; в мини-аппе повар Склад видит. Решить отдельно:
      открыть повару Склад на `/web` или убрать пункт из бара.
    - **Как проверено:** `tsc --noEmit`, `next build`. Playwright (Chromium)
      на собранном `out/` с подставленными ответами API, **и мини-апп (`/`),
      и `/web/`**, 390px и 1280px, руководитель с 3 непрочитанными и повар
      без уведомлений; прокликан нижний бар: Главная, ТТК, Акты, Склад
      (+ переключение на "Списание"), Банкет. Замер
      `getBoundingClientRect().top` шапки / показателей / плиток до и после
      разворота-сворота шторки совпадает в обоих местах (390px руководитель:
      0 / 284 / 475; повар без уведомлений: шторки нет, 0 / 224 / 415).
      После выкладки `/`, `/cabinet/`, `/web/`, `/banquet/`,
      `/waiter-guide/`, `/print-admin/` — 200, новые чанки (`kd-ftab`,
      `kd-strip-slot`, `kd-hero-title`) отдаются с прода. Живьём в Telegram
      и в APK не смотрели — за пользователем.
  - **Решение по вопросу из `8bde16d` (Склад пустой у повара на /web):
    открыть повару Склад на /web**, как в мини-аппе — не убирать кнопку
    из бара.
  - **Живой отзыв пользователя (2026-09-26, ещё одна партия правок):**
    1. **Вкладыши папки Склада выглядят некрасиво** — сейчас, по словам
       пользователя, это просто неаккуратные геометрические фигуры, не
       похоже на настоящий вкладыш папки. Идею (активный вкладыш выше и
       светлее, неактивный ниже/темнее/наклонён, содержимое "выезжает
       как лист") не менять — улучшить именно исполнение, чтобы
       выглядело естественно и аккуратно, не грубыми геометрическими
       блоками.
    2. **На вкладках "Чек-лист" и "План" (видны у повара — в мини-аппе и
       в APK) баннера-шапки нет вообще, а должен быть**, так же как
       добавили на ТТК/Акты/Склад/Банкет: тот же баннер с заголовком
       раздела чуть ниже центра (например, "Чек-лист", "План
       заготовок" — можно скорректировать формулировку на месте).
    3. **Баннер (шапка с фото) — уменьшить, везде** (уточнено
       пользователем: и на Главной, и на всех вкладках-разделах — не
       только там, где нет приветствия). Сейчас высота 210px, слишком
       большой/навязчивый. Сократить на разумную величину (конкретное
       число — на усмотрение реализующей сессии, важно чтобы фото и
       текст — приветствие с аватаром на Главной, заголовок раздела на
       остальных вкладках — остались читаемыми и не обрезались).
    4. **На вкладке ТТК фильтр (плашки категорий "Все/гарнир/горячее/
       десерт/закуски" и поиск) после добавления баннера встал криво,
       смотрится некорректно.** Нужно перенести его в другое место —
       куда именно, пользователь не определил ("не знаю куда"), отдаёт
       на усмотрение реализующей сессии, единственное требование:
       фильтр должен остаться полностью рабочим (тот же живой поиск с
       debounce, что и был), просто визуально аккуратно встроен рядом с
       новым баннером, а не наезжать на него/выглядеть сломанным.
  - **СДЕЛАНО и задеплоено 2026-09-26 — решение по Складу + партия правок
    из `0a1e860`/`8a50752`, в обоих местах (мини-апп и `/web`/APK).**
    Фронтенд по-прежнему не в git: live-edit, `npm run build`, `rsync
    --delete` с шестью `--exclude` (dry-run чистый). Бэкапы —
    `*.bak-redesign3-20260926-131118`.
    - **Склад повару на `/web`** — в `src/app/web/page.tsx` снято условие
      "только руководителям", Склад рендерится всем ролям, как в мини-аппе.
      Бэкенд повара и так пускал (`/api/writeoff` — `requireRole(...,
      'cook')`, `/api/purchase` без ограничения ролей), менять не пришлось.
    1. **Вкладыши папки Склада переделаны** (`features/Warehouse.tsx`),
       идея та же. Вместо трапеций на `clip-path` — вкладыш со скруглённым
       верхом и плавными вогнутыми "юбочками" у основания (псевдоэлементы
       на `radial-gradient`), так что активный вкладыш без шва перетекает
       в кромку папки; кромка — полоса цвета активного вкладыша, мягко
       растворяющаяся в фоне (вместо резкой плашки). Активный — выше,
       светлее, с тонким бликом сверху и мягкой тенью; неактивный — на
       5px ниже, темнее, чуть отклонён назад (`rotateX(10deg)` вместо
       18), заходит под соседа. Переключение — упругий подъём вкладыша
       (~380ms) + лист содержимого выезжает из папки (~360ms).
    2. **Шапка на "Чек-листе" и "Плане"** — "Чек-лист смены"
       (`features/Checklist.tsx`) и "План заготовок" (`features/Plan.tsx`).
       Прежние `<h1>` убраны, строка с цехом (и переключатель дат в
       Чек-листе) — сразу под шапкой. Шапка есть и на служебных экранах
       этих вкладок ("Смена завершена", "Цех не назначен").
    3. **Шапка уменьшена везде: 210 → 168px** (одна переменная `--hero-h`
       в `layout/SectionHero.tsx` — Главная и все разделы сразу),
       скругление 28 → 26px. Аватар 70px и имя/должность на Главной
       помещаются с запасом (~49px сверху и снизу), заголовки разделов не
       обрезаются; шторка уведомлений подстроена под новую высоту.
    4. **ТТК: поиск и фильтр — одна стеклянная панель на стыке с шапкой**
       (`features/Search.tsx`): поле поиска + справа за разделителем
       кнопка фильтра цехов (подсвечивается, пока фильтр открыт; точка —
       если выбран цех); панель наплывает на нижний край шапки
       (blur, как шторка). Плашки цехов (по кнопке) и категорий
       ("Все/гарнир/горячее/…") — строками сразу под панелью. Логика не
       менялась: тот же `debouncedSearch`, `setShowSectionFilter`,
       `setActiveSection`, `setActiveCategory` — только разметка/стили.
    - **Как проверено:** `tsc --noEmit`, `next build`; playwright
      (Chromium) на собранном `out/` с подставленными ответами API — и
      мини-апп (`/`), и `/web/`, 390px и 1280px, руководитель с 3
      уведомлениями и повар без уведомлений; прокликаны Главная, ТТК
      (+ открытие фильтра цехов), Смена, План (повар), Акты, Склад
      (+ "Списание"; у повара на `/web` теперь открывается), Банкет
      (руководитель). Шторка: положение шапки / показателей / плиток до и
      после разворота совпадает (390px руководитель: 0 / 242 / 433; повар
      без уведомлений — шторки нет: 0 / 182 / 373). После выкладки `/`,
      `/cabinet/`, `/web/`, `/banquet/`, `/waiter-guide/`, `/print-admin/`
      — 200, новые чанки (`kd-ttk-search`, `kd-folder-edge`,
      `--hero-h: 168px`) отдаются с прода. Живьём в Telegram/APK не
      смотрели — за пользователем.
  - **Живой отзыв пользователя (2026-09-26) — вкладыши Склада всё ещё не
    те, плюс новая роль "Шеф" (view-only + мониторинг):**
    1. **Вкладыши "Закуп"/"Списание" — доработка, не с нуля.** Стало
       лучше после `7588638`, но всё ещё не то — пользователь прислал
       скриншот: под самими вкладками торчат лишние тёмные треугольные/
       трапециевидные обрезки, ломающие силуэт "вкладыша папки" — вместо
       гладкого перехода получается рваный край с клиньями по бокам
       снизу (видно в месте стыка вкладышей с панелью под ними). Похоже,
       текущая техника отрисовки — несколько слоёв/фигур с обрезкой
       углов, которые кое-где вылезают за пределы задуманной формы.
       Стоит пересмотреть подход целиком (единая маска/`clip-path`/SVG-
       форма на весь вкладыш, а не сборка из нескольких кусков) — не
       просто подправить цвета/радиусы поверх той же техники, раз она
       уже дважды давала артефакты.
    2. **Новая роль "Шеф" — везде только просмотр, без права
       редактирования.** Тот же набор разделов, что у остальных ролей
       (Главная, ТТК, Акты, Склад, Чек-лист/План, Банкет), но без
       единой возможности что-то создать/изменить/сохранить/списать —
       во всех этих разделах, не только на каком-то одном экране. Не
       новый урезанный набор вкладок, а те же экраны в режиме
       "только просмотр".
    3. **Главная у "Шефа" — дашборд/мониторинг вместо обычных плиток.**
       Согласовано с пользователем: показывать сводку в реальном
       времени —
       - статус цехов (то, что уже есть в Управление → Мониторинг,
         сейчас недоступно обычным сотрудникам) — вынести на Главную
         "Шефа" как основной виджет;
       - сколько чек-листов на сегодня выполнено / ещё нет, по всем
         поварам сразу;
       - статус плана заготовок на сегодня (выполнен/в процессе);
       - последние записи по Актам разделки (короткая лента, без захода
         в сам раздел).
       Точная вёрстка виджетов — на усмотрение реализующей сессии, суть
       — дать шефу общую картину "что как происходит" одним взглядом на
       Главной, всё кликабельно только на просмотр, без действий.
    4. **Тестовый аккаунт для роли "Шеф" пользователь заведёт сам** (ещё
       одна запись сотрудника под его же Telegram-аккаунтом, но с ролью
       "шеф") — от реализующей сессии тут действий не требуется, только
       сама функциональность роли.
    5. **Где реализовывать:** основная цель сейчас — мини-апп
       (`/cabinet`), там сейчас реальный вход сотрудников по ролям через
       Telegram. Если что-то естественно попадает и в APK/`/web` за счёт
       общих компонентов (как Главная через `HomeScreen.tsx`) — хорошо,
       но специально дублировать вручную в APK не обязательно для этой
       задачи, раз там пока только один тестовый аккаунт входа.
  - **СДЕЛАНО и задеплоено 2026-09-26 — вкладыши Склада + роль "Шеф"
    (`31d1df7`).** Фронтенд: live-edit + `npm run build` + `rsync --delete`
    с шестью `--exclude` (dry-run чистый), бэкапы `*.bak-chefview-20260926-135411`.
    Бэкенд (`/root/kitchendesk/backend`): правки + `pm2 restart kitchendesk`,
    бэкапы `*.bak-chefview-20260926-135236`. Ни то ни другое по-прежнему не в git.
    - **Решения пользователя по ходу (на вопрос сессии):**
      - "Шеф" — это **существующая роль `chef` ("Шеф-повар")**, переделанная
        в "только просмотр", а не новая роль. Следствие, о котором
        предупреждали: действующий шеф-повар ОКО (Азиз, id 64) и
        `demo_chef` потеряли Управление, правку ТТК, списания и т.п.
      - Тестовый аккаунт — **отдельный Telegram-аккаунт**: второй записи
        под тем же Telegram быть не может, `users.tg_id` — UNIQUE (один
        Telegram = одна запись сотрудника). Заводится как обычно в
        Управление → Сотрудники с ролью "Шеф-повар".
    1. **Вкладыши Склада — подход переделан целиком**
       (`features/Warehouse.tsx`). Каждый вкладыш — **один элемент с
       одним контуром** `clip-path: path(...)`: вогнутые изгибы у
       основания, прямые бока и скруглённый верх — одной фигурой, без
       трапеций и без псевдоэлементов-"юбочек" (оба прошлых варианта
       собирали форму из кусков — отсюда клинья на стыках). Ширина
       вкладыша фиксированная (156px вместе с изгибами — `path()` задаётся
       в пикселях, для "Закуп"/"Списание" хватает с запасом). Тень —
       `drop-shadow` на обёртке-кнопке, чтобы её не обрезало. Кромка папки
       под вкладышами сверху **сплошная** цвета активного вкладыша, ниже
       мягко растворяется; неактивный вкладыш стоит на 5px ниже, темнее,
       слегка отклонён назад (`rotateX(8deg)`) и основанием уходит **за**
       кромку — снизу у него физически нечему торчать. Активный — выше,
       светлее, с бликом по верхнему краю, изгибами перетекает в кромку.
       Анимация прежняя (упругий подъём вкладыша + лист выезжает).
       **Как выглядит** — скриншот (сверху активен "Закуп", снизу
       "Списание"): `print-agent/docs/warehouse-folder-tabs-2026-09-26.png`.
    2. **Роль `chef` — только просмотр, везде.**
       - **Бэкенд — одна граница вместо правки ~100 `requireRole`:** в
         `src/middleware/auth.js` (`chefReadOnlyBlocked`) любой не-GET
         запрос от `chef` → 403 "Роль «Шеф-повар» — только просмотр", в
         обеих ветках входа (Telegram initData и Bearer `/web`). Разрешено
         только то, что данные не меняет: прочитать уведомление, поддержка
         (`/api/support/`), печать ТТК на чек (`/api/ttk/:id/print`),
         экспорт/отправка отчёта в чат (`/api/exports/`). Проверено
         прогоном функции на наборе запросов (chef: GET — пропуск; POST
         акта/списания, PUT закупа, архив/удаление ТТК, завершение смены —
         403; sushef/cook — без изменений). В боте у `chef` убрано
         управление группами заведения (`canManageRestaurantGroups` и
         привязка группы) — Telegram-уведомления руководителям шеф
         по-прежнему получает.
       - **Фронтенд — кнопок действий просто нет** (`utils/roleAccess.ts`
         → `isReadOnlyRole`): ТТК — нет правки/архива/удаления (печать и
         экспорт остались); Чек-лист — выбор цеха чипами (своего цеха у
         шефа нет), дата листается, отметки не нажимаются, нет "Добавить
         позицию"/правки/"Завершить смену"; План — вместо формы отправки
         просмотр заполненного плана по цеху и дате (сегодня/завтра/
         послезавтра, позиции, кол-во, отметки, заметка); Акты — нет
         "Нового акта" и утверждения/возврата/отклонения; Склад → Закуп —
         нет Каталога/Добавить/правки/удаления/отправки бланка/доп.
         закупа; Списание — нет "Нового акта списания", повторной
         отправки и удаления (история видна); Банкет — нет "Нового
         банкета", удаления, добавления/удаления блюд, отправки PDF
         (скачать PDF можно); Профиль — нет входа в Управление.
       - **Нижний бар шефа:** Главная, ТТК, Смена, План, Акты, Склад,
         Банкет, Профиль (8 пунктов, без Управления); вкладка `admin`
         шефу закрыта и по deep-link. Раньше `chef` считался
         руководителем и Смена/План ему были закрыты — теперь открыты.
    3. **Главная шефа — мониторинг** (`dashboard/ChefOverview.tsx` внутри
       общего `HomeScreen.tsx`, шапка и шторка уведомлений те же).
       Новый эндпоинт `GET /api/admin/overview` (`api/admin.js`), обновление
       раз в минуту. Виджеты: (а) три показателя — **Чек-листы** "X/N цехов
       выполнено" (всё отмечено или смена закрыта), **План сегодня** —
       % выполненных позиций по всем цехам, **На завтра** — сколько цехов
       уже заполнили план; (б) **Статус цехов** — по каждому цеху повара,
       полоса выполнения X/Y, статус (Выполнен/В процессе/Не начат/Нет
       плана), "Смена закрыта", заполнен ли план на завтра; (в)
       **Последние акты** — 5 последних: №, продукт, повар·цех, время,
       статус. Тап ведёт в соответствующий раздел (на просмотр).
       Скриншот: `print-agent/docs/chef-home-2026-09-26.png`.
       Оговорка: отдельной записи "смена закрыта" в БД нет — берётся
       уведомление `shift_report` за сегодня с названием цеха (появляется,
       только если в заведении включено это уведомление); "чек-лист" и
       "план на сегодня" в системе — один и тот же ключ
       `plan_<цех>_<дата>`, поэтому "План сегодня" — это выполнение
       позиций, а заполненность считается по плану на завтра.
    4. Тестовый аккаунт — за пользователем (см. решение выше).
    5. **Где:** всё в мини-аппе; в APK/`/web` попало то, что общее
       (Главная-мониторинг, Чек-лист/План/Склад/Банкет/Акты мини-аппа,
       бар, запрет на бэкенде), плюс на `/web` шефу скрыто Управление в
       меню. Отдельно не дорабатывал `/web`-версию Актов (`WebActs.tsx` —
       кнопка "Новый акт" там у шефа видна, но бэкенд её отклонит).
    - **Как проверено:** `tsc --noEmit`, `next build`; SQL сводки прогнан
      на живой базе ОКО (6 цехов, 5 последних актов). Playwright на
      собранном `out/` с подставленными ответами API за шефа (мини-апп и
      `/web`, 390px): Главная-мониторинг, Смена, План, Акты, Склад
      (Закуп/Списание), Банкет, ТТК — кнопок действий нет, выбор цеха
      работает; повар без изменений (Смена/План/Склад как раньше);
      шторка уведомлений по-прежнему ничего не двигает. После выкладки
      `/`, `/cabinet/`, `/web/`, `/banquet/`, `/waiter-guide/`,
      `/print-admin/` — 200, новые чанки отдаются, бэкенд после рестарта
      отвечает (401 без авторизации, как и должно). Живьём под реальным
      шефом в Telegram не смотрели — за пользователем.
  - **Живой отзыв пользователя (2026-09-26) — карточка блюда (ТТК):
    навести порядок в кнопках действий + подтянуть калькулятор пересчёта
    до уровня проекта "Жигули".** Пользователь прислал живой скриншот
    карточки под су-шефом (см. описание ниже, файла на диске нет,
    описание — источник истины).
    1. **Сейчас на карточке два разрозненных кластера кнопок** — наверху
       "Редактировать" + "···" (в нём "Архивировать"/"Удалить"), отдельно
       ниже, рядом с меткой "СОСТАВ" — "Калькулятор"/"Печать"/"Экспорт".
       **Свести все шесть действий в одно место, одной группой** —
       видимость по ролям не менять (повар — только Калькулятор и
       Печать; су-шеф — все шесть; шеф — Калькулятор/Печать/Экспорт, без
       действий редактирования — уже сделано в `04ac7eb`). Как именно
       визуально сгруппировать — на усмотрение реализующей сессии,
       главное — не два места, а одно.
    2. **Калькулятор пересчёта — довести до паритета с "Жигулями".**
       Сейчас в KitchenDesk только два режима: "× Множитель" и "От
       продукта". В "Жигулях" (тот же репозиторий, ветка `zhiguli`,
       `web/src/lib/calc.ts` + `web/src/components/RecipeDetail.tsx`,
       строки с `CalcMode`/`modes`) их четыре, теми же чипами-
       переключателями:
       - `multiplier` — "× Множитель" (уже есть)
       - `ingredient` — "От продукта" (уже есть)
       - `portions` — "Порции": пересчитать так, чтобы выход в порциях/
         штуках (`recipe.yield_count`) стал заданным числом
       - `weight` — "Выход": пересчитать так, чтобы выход по весу/объёму
         (`recipe.yield_weight` + `recipe.yield_unit`, кг/г/л/мл со
         сверкой размерности) стал заданным
       Режим "Порции" неактивен (серый, с подсказкой-причиной), если в
       конкретной карте не указан `yield_count`; "Выход" неактивен, если
       не указан `yield_weight` — та же логика, что в `calc.ts`
       (`coefficient()`, `toBase()`, `isActive()`, `scaleValue()`).
       **Логику пересчёта можно переносить из `calc.ts` почти один в
       один** — это чистая математика, не завязанная на специфику
       Жигулей, только сам движок пересчёта (не переносить модульную
       гейтовку `useModule('recalc')` — это платный тумблер Жигулей,
       в KitchenDesk своя система прав, не при чём).
       У всех ролей режим пересчёта работает одинаково — правка не
       касается того, кто что видит на карточке, только самого набора
       режимов пересчёта.
  - **СДЕЛАНО и задеплоено 2026-09-26 — карточка ТТК (`29eff44`).**
    Один файл: `src/components/features/Search.tsx` (карточка `TtkDetail`
    и `CalculatorModal`) в `/home/oko-kitchen/oko-kitchen/oko-frontend`;
    фронтенд не в git — live-edit + `npm run build` + `rsync --delete` с
    шестью `--exclude` (dry-run чистый), бэкап
    `Search.tsx.bak-ttkactions-20260926-145459`. Бэкенд не трогали — поля
    `yield_count` / `yield_weight` / `yield_weight_unit` уже приходят в
    карточку (в базе ОКО заполнены у 126 и 331 карт из 597; единицы
    выхода — кг и л). Карточка общая — изменения сразу и в мини-аппе, и
    на `/web`.
    1. **Одна панель действий** сразу под названием блюда (вместо двух
       кластеров — наверху и у "Состава"): ряд равных кнопок "иконка над
       подписью" в одной плашке — **Калькулятор · Печать · Экспорт | Править
       · Ещё** (за разделителем — действия редактирования; "Архивировать"
       и "Удалить" — в "Ещё", внутри той же панели). Видимость по ролям
       не менялась (те же `canPrint`/`canExport`/`canEdit`): повар —
       Калькулятор+Печать, су-шеф/админ — все пять кнопок (6 действий),
       шеф-повар — Калькулятор/Печать/Экспорт. Кнопка "Калькулятор"
       подсвечивается зелёным, пока пересчёт активен. У метки "Состав"
       кнопок больше нет.
    2. **Калькулятор — 4 режима, как в Жигулях** (`calc.ts` перенесён
       почти один в один: `calcNum`≈`num`, `calcToBase`=`toBase`,
       `calcIsActive`=`isActive`, коэффициент — в `computeCalc`):
       "× Множитель", "От продукта" (было "От количества" — переименовано
       как в Жигулях, логика KitchenDesk та же), **"Порции"** (k = нужно
       порций / `yield_count`), **"Выход"** (k = нужный выход / `yield_weight`
       через базовые единицы; выбор единицы той же размерности, что в
       карте — кг/г или л/мл, кг↔л не пересчитываются). Если в карте нет
       нужного поля — чип серый и неактивный, причина написана под
       чипами ("в карте не указан выход в порциях / по весу"; `title`
       на телефоне не виден, поэтому текстом). Чипы 2×2 на телефоне, 4 в
       ряд на широком экране. В "Результате" добавлена строка "Выход:
       было → стало" (вес и штуки) + коэффициент; на самой карточке при
       активном пересчёте плашки выхода показывают пересчитанные значения
       (как в Жигулях). Печать по-прежнему берёт пересчитанный состав.
       Гейтовку `useModule('recalc')` не переносили.
    - **Как проверено:** `tsc --noEmit`, `next build`; playwright на
      собранном `out/` с подставленными ответами API (карта с выходом
      1,5 кг / 6 шт и карта без выхода), 390px: состав кнопок в панели —
      су-шеф "Калькулятор, Печать, Экспорт, Править, Ещё" (в "Ещё" —
      Архивировать/Удалить), повар "Калькулятор, Печать", шеф "Калькулятор,
      Печать, Экспорт"; "Порции" 12 → ×2 (состав удвоен, выход 3 кг /
      12 шт); "Выход" 3 кг → ×2; 750 г → ×0,5 (1,5 → 0,75 кг, 6 → 3 шт);
      у карты без выхода "Порции"/"Выход" disabled. После выкладки `/`,
      `/cabinet/`, `/web/`, `/banquet/`, `/waiter-guide/`, `/print-admin/`
      — 200, новый чанк отдаётся с прода. Скриншоты (су-шеф, повар, шеф,
      режим "Выход", карта без выхода):
      `print-agent/docs/ttk-card-actions-calc-2026-09-26.png`. Живьём в
      Telegram не смотрели — за пользователем.
  - **Живой отзыв пользователя (2026-09-26) — калькулятор пересчёта:
    убрать модальное окно, сделать выезжающей панелью.** Проверил
    `bdf8b69` вживую, работает, но формат не тот:
    - **Сейчас** нажатие "Калькулятор" открывает **модальное окно**
      (затемнение + карточка по центру экрана), внутри — переключатель
      режимов, поле ввода, коэффициент, и **отдельно свой список
      ингредиентов "Было/Стало"**.
    - **Нужно:** модальное окно убрать полностью. Вместо него по нажатию
      "Калькулятор" **снизу выезжает панель** (та же механика, что у
      шторки уведомлений на Главной — не полноэкранное затемнение, а
      выезжающий снизу блок) — внутри неё **только** переключатель
      режимов (× Множитель / От продукта / Порции / Выход), нужное поле
      ввода и получившийся коэффициент. **Список "Было/Стало" внутри
      этой панели не нужен** — вместо него **основная таблица "Состав"
      на самой карточке** должна сама показывать пересчитанные значения,
      пока калькулятор активен (сейчас, судя по скриншоту `bdf8b69`,
      таблица "Состав" на карточке показывает только один столбец
      "Количество" всегда, было/стало было только внутри модалки — это
      нужно перенести: пока активен ненулевой коэффициент, "Состав" на
      карточке показывает пересчитанные значения — по аналогии с тем,
      как это уже было реализовано внутри модалки, просто теперь прямо
      в основной таблице, а не в дублирующей).
    - Идея пользователя: "калькуляция" (состав карточки) и так уже
      обновляется автоматически при активном коэффициенте — значит
      отдельный список внутри калькулятора избыточен, дублирует то, что
      и так видно на самой карточке.
    - Строка "Выход: было → стало" (которая уже есть, см. `bdf8b69`) —
      можно оставить внутри выезжающей панели, это не список
      ингредиентов, а сводка по самому выходу, не дублирует состав.

## Как теперь вносить изменения в агента

1. Править `oko-shelf-life/agent/print-agent.js` в этом репозитории
   (ветка `claude/oko-waiter-guide-xlsx`) — обязательно поднять
   `AGENT_VERSION` на новое значение.
2. Коммит и пуш в ту же ветку.
3. Передать в сессию с доступом к серверу задачу на деплой: обновить на
   сервере `oko-shelf-life/agent/print-agent.js` (и
   `oko-shelf-life/backend/oko-shelf-life-api.js`, если он тоже менялся),
   перезапустить процесс `kitchendesk`.
4. Агент на моноблоке подхватит новую версию сам — при следующем
   запуске/перезагрузке моноблока, либо в течение 6 часов автоматически.
   Пользователю вручную скачивать и переносить файл больше не нужно
   (кроме самого первого раза, когда механизм самообновления только
   появился).

## Где искать подробности

- `oko-shelf-life/README.md` — полный технический референс: печать
  этикеток, самообновление, форматирование чека, история найденных багов
  и тюнинга (кодовая страница, отступ перед обрезкой и т.п.).
- `oko-checklist-print/README.md` — фича печати чек-листа смены при
  сохранении плана + завершение сканом QR: исходный план, что по факту
  изменилось при реализации (единый процесс вместо двух серверов), и
  история правок по итогам живых тестов на реальном принтере.
- `oko-shelf-life/agent/print-agent.js` — сам код агента.
- `oko-shelf-life/backend/oko-shelf-life-api.js` — роуты очереди печати
  (включая роуты самообновления агента).
- `print-agent/docs/` — файлы для пользователя, не для кода: PDF-инструкция
  по чековому принтеру, полная инвентаризация функций KitchenDesk (.md и
  .xlsx, для тарифов), печатные версии общего плана работ (PDF + PNG,
  см. п.23 в хронологии выше).

## Кому и зачем этот файл

Пользователь ведёт эту работу через две параллельные сессии Claude: одну
без доступа к серверу (пишет и тестирует код локально, координирует), и
другую — с реальным доступом к серверу, которая деплоит. Обе сессии не
помнят предыдущих разговоров друг друга. Этот файл — общая точка входа
для любой из них (или следующей, если эти тоже закончатся), чтобы не
начинать каждый раз с нуля.

**Важно (с 2026-09-22):** несмотря на название, файл давно вышел за рамки
буквально "print-agent" — здесь же координация по мобильному приложению,
security-аудиту входа, тарифам и контент-плану KitchenDesk в целом. Это
сознательный выбор — не плодить несколько несинхронизированных
файлов-точек-входа, а держать один. Если следующая сессия ищет "с чего
всё началось" по печати — это по-прежнему первые ~17 пунктов хронологии;
всё остальное (п.18+) — более широкий контекст проекта.

**Если вы — новая сессия, продолжающая после перерыва:** прочитайте
"Открытые вопросы / что в работе" (сразу после хронологии) — там актуальный
на момент последней правки список того, что реально не завершено и ждёт
следующего шага, без нужды перечитывать всю хронологию целиком.
