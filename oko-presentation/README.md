# OKO / KitchenDesk — скриншоты для презентации (для потенциальных клиентов)

Временная папка-склад (как `oko-order-relay/` и `oko-iiko-integration/`) —
сюда нужны **реальные скриншоты** реальных экранов приложения. Я (сессия
Claude Code on the web) не могу сама зайти на `kitchendesk.chefplan.ru` —
сеть, в которой я работаю, блокирует выход на этот хост политикой (не логин/
пароль, а сетевой уровень). GitHub мне доступен нормально — план: сервер
делает скриншоты и коммитит их сюда, я собираю из них интерактивный веб-тур.

**Обновление (2026-08-12, сессия с доступом к серверу):** скриншоты сняты
(Playwright, живой demo-стенд на ресторане 6) и закоммичены в
`screenshots/web/`, `screenshots/bot/`, `screenshots/notes/` — см. подробный
отчёт `REPORT-2026-08-12-screenshots.md`. Список файлов и разделов ниже
**обновлён по факту** (актуальный порядок и имена — то, что реально
закоммичено); исходный план 1:1 сохранён в отчёте для сравнения. Главная
неожиданность: `/cabinet/` — это **не форма заказа**, а мобильная версия
той же самой веб-админки (см. отчёт, раздел «Главная неожиданность»).

**Важно про аудиторию:** это не внутренняя техническая документация — это
презентация для потенциальных клиентов/ресторанов, которые решают,
подключаться ли к KitchenDesk. То есть нужен **полный обзор приложения**, не
только iiko-часть (она — один из разделов, не вся презентация), и снимать
стоит в состоянии, которое выглядит убедительно (заполненные реальными
данными экраны, не пустые списки) — раз ресторан 6 уже наполнен тестовыми
данными (10 сотрудников, несколько ТТК с категориями), показывать лучше на
нём.

## Шаг 0 — сначала список разделов (сделать первым)

Прежде чем снимать скриншоты по конкретному списку ниже, пройтись по
навигации веб-админки и мини-аппа и **зафиксировать полный список разделов**,
которые вообще есть в приложении (пункты меню/маршруты). Список ниже — то,
что уже известно из предыдущей работы, но он наверняка неполный (например,
неизвестно, есть ли отдельный дашборд/статистика, управление сотрудниками
как отдельный раздел, склад/остатки, настройки заведения и т.д.). Дописать
в отчёт то, что нашлось сверх списка ниже — это и определит, каких
скриншотов не хватает.

## Что можно снять автоматически (Playwright/headless-браузер на сервере)

Обычные веб-страницы, Telegram-контекст не нужен:

- `https://kitchendesk.chefplan.ru/web/` — веб-админка.
- `https://kitchendesk.chefplan.ru/cabinet/` — форма заказа (мини-апп, но
  без Telegram WebApp SDK, должна открываться как обычная страница).

### Веб-админка (`oko-presentation/screenshots/web/`) — СНЯТО

Viewport 1440×900, PNG. Реальный порядок и имена файлов (см. отчёт про то,
что из первоначального плана пришлось переставить/переименовать и почему):

1. `01-login.png` — экран входа (PIN/Пароль).
2. `02-dashboard.png` — Главная (дашборд администратора: счётчики ТТК/цехов/
   поваров, быстрые действия, статус цехов realtime, график на неделю).
3. `03-restaurants-list.png` — список ресторанов (панель суперадмина).
4. `04-restaurant-card-iiko.png` — карточка ресторана 6 + блок «Подключение
   к iiko» (стенд, статус проверки, кнопки «Импорт ТТК»/«Импорт графика»).
5. `05-iiko-import-toast.png` — результат нажатия «Импорт ТТК» (тост со
   сводкой).
6. `06-ttk-list.png` — список ТТК (11 рецептов, чипы категорий).
7. `07-ttk-card-manual.png` — карточка обычного блюда («Стейк Рибай»),
   полностью редактируемая — базовый рабочий процесс без iiko вообще.
8. `08-ttk-card-iiko-locked.png` — карточка iiko-блюда («картофель
   отварной») в режиме редактирования — заблокированное (muted) поле
   состава + предупреждение «Ингредиенты этого блюда синхронизируются с
   iiko — редактировать их в KitchenDesk нельзя».
9. `09-schedule.png` — общая таблица графика смен на неделю (админ).
10. `10-employees.png` — раздел «Сотрудники» (Управление → Люди).
11. `11-workshops.png` — раздел «Цеха» (Управление → Люди).

Полный список разделов, найденных по «Шагу 0» (включая то, что НЕ снято и
почему) — в отчёте.

### Мобильная версия / мини-апп (`oko-presentation/screenshots/bot/`) — СНЯТО, но не то, что планировали

`/cabinet/` оказался **не формой заказа**, а мобильной версией той же самой
веб-админки (тот же JWT из `/web-login`, тот же контент, просто мобильная
раскладка с нижним таб-баром) — см. отчёт. Снято по аналогии с веб-частью:

1. `01-mobile-dashboard.png` — та же Главная, мобильная раскладка.
2. `02-mobile-ttk-list.png` — список ТТК, мобильная раскладка.
3. `03-mobile-ttk-card.png` — карточка блюда, мобильная раскладка.

### `oko-presentation/screenshots/notes/` — находки, не для тура

Не часть клиентского тура, приложено как документация находки (см. отчёт):

- `cabinet-unauthenticated-is-marketing-landing-with-placeholder-content.png`
  — что видно на `/cabinet/` БЕЗ авторизации: публичный лендинг-регистрация
  с тестовыми заглушками в отзывах/FAQ («Тест отзыв», «Ййййй», «Тест
  вопрос») — не готово к показу клиенту как есть.
- `notifications-settings-bonus-feature.png` — раздел «Уведомления»
  (Управление → Настройки) — детальная настройка каждого типа уведомления
  в Telegram, с индивидуальным временем для тех, что по расписанию.

## Что автоматически не снять — нужны твои реальные скриншоты

Сообщения самого бота внутри Telegram — интерфейс Telegram-клиента, не
веб-страница, скриншотить нечем на сервере. Эти несколько экранов лучше снять
вручную с телефона и прислать в чат отдельно (не через эту папку):

- Сообщение с заказом в поварской группе (после отправки формы).
- Кнопка «✅ Принято» до нажатия.
- Как выглядит сообщение после нажатия «Принято» (кто принял, с тегом повара).
- Подтверждение в исходном топике (цитата заказа).

## Формат

- PNG, не сжимать агрессивно (нужно читаемо, не мыльно), но и не оставлять
  совсем сырые огромные файлы — среднее качество достаточно.
- Снимать в состоянии с реальными заполненными данными, не пустые экраны —
  это презентация для клиентов, должно выглядеть убедительно.
- Если что-то не получается снять или раздел ещё пустой (например, отчёты по
  продажам, которые на demo iiko всё ещё нулевые) — пропустить, указать в
  отчёте, что и почему, не выдумывать данные ради красивой картинки.

## Отчёт

Полный список найденных разделов (по «Шагу 0»), что снято, что пропущено и
почему, куда закоммичено. Если по дороге заметите что-то, что стоит
подчеркнуть в презентации клиенту (сильная сторона, наглядная деталь) —
стоит упомянуть отдельно, пригодится для текста тура.
